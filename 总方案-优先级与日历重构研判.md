# 调休管家 总方案：优先级收口 · 判重规则 · 日历双状态重构研判

> 本文汇总前三份文档的结论，并新增两项需求（区间判重、日历双状态重构）。
> 关联文档：`代码审计-冗余与安全风险.md`、`修复方案.md`、`内置管理员改造方案.md`

---

## 🔒 决策状态（截至本次确认）

| 决策 | 结论 | 状态 |
|---|---|---|
| E1 其他转休互斥口径 | ① 互不干扰（MANUAL 内部同人同日仅一条；与加班转休互不干扰） | ✅ 已定 |
| **E2 跨模块判重** | **需要判重**：加班转休 ↔ 调休使用 合并到同一套**区间重叠**判定（推翻原先"不跨模块"的建议） | ✅ 已定（本次变更） |
| E3 端点相接 | 不算重叠（15–17 与 17–19 允许） | ✅ 已定 |
| E4 日历重构时机 | 按 P0→P5 顺序，与 P2-4、P1-3 合并做 | ✅ 已定 |
| D1 | `WriteGate` 走向 | 三层方案：① DB `active_key` 唯一索引 ② 删 3s 冷却 + `tryLock(3s)`→429 ③ 保留程序判重 | ✅ **已上线（2026-09-11）** |
| D2 | 系统配置页 | **A 接上业务** | ✅ 已定 |
| D3 | 登出语义 | **B 只退当前设备（令牌黑名单）** | ✅ 已定 |
| B1 | 内置管理员默认密码 | **B 环境变量** | ✅ 已定 |
| B2 | 内置管理员豁免登录锁定 | **A 豁免**（须补 IP 限流） | ✅ 已定 |
| B3 | 日志保留 admin 操作 | **A 保留** | ✅ 已定 |
| B4 | 管理员前端不显示 | **A 纯隐藏** | ✅ 已定 |

---

# 第一部分 · 全量需求优先级总表

排序原则：**先堵安全和数据正确性的洞，再谈加固，最后做重构。**
每行都标注了「正向影响」与「负向影响」，负向为空表示无副作用。

## P0 · 阻断风险（不改业务语义，建议当天完成）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P0-1 ✅ | 安全基线 | 密钥/DB 口令不得硬编码 | JWT 密钥与 `root/root` 明文写在 `application.yml`，且是明显的"待替换默认值" | 改环境变量注入 + 启动自检，检测到默认/空值**拒绝启动** | 从根上堵死"拿到源码即可伪造 admin 令牌" | 攻击者无法再离线签发管理员令牌 | 上线多一步（必须设环境变量）；忘设则**启动失败** |
| P0-2 ✅ | 并发可用性 | 一个人刷请求不能拖垮全站 | `WriteGate.acquire()` 用 `lock.lock()` **无限等待**，拦截器又先于权限校验抢锁 | 改 `tryLock(3s)`，超时抛 429 快速返回；**并删除写后 3s 冷却**（实测冷却才是"全场干等"主因） | 线程池不会被长期占用 | 读请求不再被写请求拖死；不再"莫名全站卡死要重启" | 高峰期少量写请求会被拒（需重试） |
| P0-3 ✅ | 运维安全 | 生产不得自动改表结构 | `ddl-auto: update` 启动即改表、无回滚 | 改 `validate` | 表结构变更回到可控状态 | 结构漂移从"静默自动改"变成"启动即报错" | 若线上已有漂移，必须先手动修结构才能启动 |

> **P0 三项已于 2026-09-11 全部上线**（含 D1② 写锁改造）。

## P1 · 数据正确性 & 权限（本批是"钱"和"权"）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P1-1 | 权限 | 强制改密必须后端说了算 | **仅前端跳转**，后端零校验，可绕过一直用默认口令 | `JwtFilter` 拦截，只放行 `me`/`change-password`/`logout` | 默认口令不再是后门 | 堵住唯一可绕过权限的缺口 | 无（正常用户无感） |
| P1-2 | 权限 | 撤权要当场生效 | `assignRoles` 不 bump `tokenVersion`，而授权用令牌内嵌 roles → **最长 24h 仍按旧角色放行** | 角色集合**确实变化**时 bump `tokenVersion` | 撤权/降权即时生效 | 辞退、降职不再有 24 小时窗口 | 对方被强制重新登录 |
| P1-3 | **数据** | **调休录入必须防重复** | **完全没有查重**（`LeaveService.createOne` 直接 save + 扣余额） | 同人同日区间重叠校验 | 避免**重复扣减余额** | 防止真金白银的数据错误 | 历史已存在的重叠记录需先清理 |
| P1-4 | **数据** | **加班「其他转休」防重复** | `if (!isManual(mode))` 才查重 → MANUAL **不校验** | 同人同日仅允许一条 | 与"加班转休"口径统一 | 堵住重复入账 | 需先确认与加班转休的互斥口径（见第二部分） |
| P1-5 | **数据** | 加班判重口径不足 | 只比"**相同起始时间**"，15–17 与 14–16 这类**包含/交叉漏判** | 改为**区间重叠**判定 | 覆盖相等/包含/交叉全部情形 | 堵住一条能重复录入的捷径 | 现存重叠数据需清理（否则报错） |
| P1-6 | 数据 | 重复提示要能指导操作 | 文案是"不能重复录入"（确定口吻） | 改"该数据可能已存在（可能由他人刚刚提交），请刷新后重试"，**并保留**员工/日期/时间定位信息 | 用户知道下一步该做什么 | 减少"为什么不让录"的困惑与人工问询 | 需前后端文案一致 |
| P1-7 ✅ | 数据 | 需要数据库层兜底 | 两张表**都没有唯一约束**，只有普通索引 | 唯一键 + 作废时清空关键字段 | 极端并发下不产生脏数据 | 应用层失效时仍有最后一道闸 | **MySQL 无法表达"区间不重叠"约束**，只能覆盖"完全相同的键"，覆盖不全。已上线：`active_key` 唯一索引，存量 427/63 条已回填 |
| P1-8 | 安全 | 异常信息不得外泄 | `"服务器内部错误：" + e.getMessage()` 直接回传 | 通用文案 + traceId，详情只进日志 | 不再泄露 SQL/类名/路径 | 攻击者拿不到内部结构信息 | 排障需查日志（用 traceId 定位） |
| P1-9 | 安全 | CORS 不得默认全开 | 默认值 `*` + `allowCredentials(true)`，漏配即对任意站点开放带凭证跨域 | 默认值改空，未配置则不注册 | 从"静默不安全"变"显性坏掉" | 消除一处静默风险 | 漏配时跨域直接不可用（这是刻意设计） |
| P1-10 | 安全 | 上传需校验 | 仅 20MB 限制，直接丢给 EasyExcel 解析 | 扩展名 + 大小 + 行数上限 | 防 zip bomb / 内存耗尽 | 资源耗尽风险显著下降 | 无 |

## P2 · 加固与配置（不做不会立刻出事，但迟早要还）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P2-1 | 安全 | 别人不能把我锁在门外 | 登录锁定**只按账号**，5 次错误即锁 15 分钟 | 增加 IP 维度限流 | 攻击者只能锁自己 | 任何人无法再"锁死 admin 让大家登不上" | 内存实现：重启清零、多实例不共享（需换 Redis） |
| P2-2 | 会话 | 退出登录要真的失效 | 只清 localStorage，令牌在有效期内**始终可用** | 新增 `POST /api/auth/logout`，bump `tokenVersion` | 凭证真正作废 | 公共电脑上退出后令牌不再被复用 | **会让该用户所有设备一起下线**（含管理员） |
| P2-3 | 运维 | 生产少两扇后门 | Swagger 部分静态资源可匿名访问；dev 开 H2 控制台 | 生产一律关闭 | 缩小暴露面 | 不再对外挂着"接口说明书"和"数据库控制台" | 生产调试需临时开启 |
| P2-4 | 配置 | 系统配置页不能骗人 | **6 个配置项存了能改，但业务代码从不读取**；系数实际硬编码 | 二选一：接上业务 / 整体下线 | 消除"以为改了其实没改"的功能误导 | 管理员改配置真生效（或页面消失，不再误导） | 选"接上"需要同步改造 3 处硬编码（与第五部分合流） |

## P3 · 死代码清理（运行时零变化，纯维护性收益）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P3-1 | 清理 | 删除死链文件 | 13 个文件约 1441 行无任何引用（`Calendar.vue`/`Dict.vue`/`UploadController`+`ExcelService`/`Dict*` 全家桶/`LeaveRequest`+`LeaveRepository`） | 按"先删叶子再删根"顺序整块删除 | 新人接手少踩坑 | 代码库更小、更可信 | 需先确认无外部脚本调用这些接口 |
| P3-2 | 清理 | 删除无调用接口与方法 | 12 个后端接口 + 10 个前端 API 方法无调用方 | 一并删除 | 接口面缩小 | 攻击面同步缩小 | 同上 |

## P4 · 内置管理员改造（功能变更，依赖 P0/P2 完成后更安全）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P4-1 | 功能 | 管理员永久内置、全部权限、密码固定、不可改角色 | ⚠️ 仅"新装时"成立；`buildMenu` 不特判 ADMIN，权限会随绑定变化；`mustChangePwd=true` 与"不允许改密"**直接冲突（会死锁）** | 加 `builtin` 标记 + `ensureBuiltinAdmin()` 每次启动归位 + `buildMenu` 对 ADMIN 特判返回全部资源 | 权限结构性恒定，永不失效 | 管理端永不失联、永不丢权限 | 固定凭据 = 必须配合 P0-1、P2-1 与网络层限制，否则成固定后门 |
| P4-2 | 功能 | 管理员不能删/冻/改自己 | `delete`/`setStatus` **无自我保护**，唯一管理员可自删致系统再也进不去 | 加 `requireNotBuiltin` + `requireNotSelf` 守卫 | 消除自锁 | 不可能再把自己关在门外 | 无 |
| P4-3 | 功能 | 管理员不参与任何计算 | ✅ **已实现**（`requireNotSystemWorker` + `ReportService.EXCLUDE_ROLE_CODES`） | 追加 `builtin` 判断作双保险 | 与角色解耦更稳 | 即使角色表异常也不污染业务数据 | 无 |
| P4-4 | 功能 | 管理员多设备同时登录互不踢 | ✅ **已实现**（登录时不 bump 管理员 `tokenVersion`） | **保持**，且严禁在启动归位时 bump `tokenVersion` | 多端在线 | 手机/电脑/平板同时可用 | 无 |
| P4-5 | 前端 | 管理员不在界面显示 | 下拉与报表已排除；**用户管理列表仍显示** | 按 B4 三选一（建议：隐藏 + 页顶一行只读提示） | 满足"不显示"且不引发误解 | 不会有人误以为账号被删而想重建 | 看不见却占着用户名，需靠提示消除困惑 |

## P5 · 日历双状态重构（探索性，见第三部分）

| 编号 | 场景 | 需求 | 现状 | 建议 | 建议优点 | 影响面（正向） | 影响面（负向） |
|---|---|---|---|---|---|---|---|
| P5-1 | 重构 | 日历收敛为 A/B 双状态 | 三类型 `LEGAL`/`WORKDAY`/`RESTDAY` + 衍生 `ADJUSTED` | `dayKind` = A/B | 概念更少、补班概念消失 | 判定链路从 4 态降到 2 态 | 需数据迁移 |
| P5-2 | 重构 | 系数可扩展、可配置 | 系数**硬编码在 3 个类里**（详见 3.2） | 抽 `(work_type, day_kind) → ratio` 映射表 + 单一出口 | 未来加班类型/系数变化无需改代码 | 与 P2-4「配置页形同虚设」一并解决 | 要同步改 3 处调用，漏一处会出现"显示与计算不一致" |
| P5-3 | 功能 | 调休录到休息日要提示 | 调休录入**完全不判断日类型** | 判定为休息日 → 抛可继续的提示 → 用户确认后放行 | 防止误在休息日扣调休 | 减少误操作 | 需前后端配合（二次确认） |

---

# 第二部分 · 区间判重规则（新增需求）

## 2.1 规则定义

**同一员工、同一天内，所有有效记录的时间区间不允许重叠。**

判定式（半开区间，端点相接不算重叠）：

```
重叠 ⇔ A.start < B.end  &&  B.start < A.end
```

对照你给的三个例子：

| 已有记录 | 新记录 | 判定 | 结果 |
|---|---|---|---|
| 15:00–17:00 | 15:00–16:00（被包含） | `15<16 && 15<17` = true | ❌ 拒绝 |
| 15:00–17:00 | 14:00–16:00（交叉） | `14<17 && 15<16` = true | ❌ 拒绝 |
| 15:00–17:00 | 14:00–18:00（包含） | `14<17 && 15<18` = true | ❌ 拒绝 |
| 15:00–17:00 | 17:00–19:00（相接） | `15<19 && 17<17` = false | ✅ 允许（连续加班可分两段录） |

> 「端点相接」我按**允许**处理。若你要"相接也算重复"，把 `<` 改成 `<=` 即可，一句话的事——但请明确。

## 2.2 决策已锁定（E1 / E2 / E3）

| 决策 | 结论 |
|---|---|
| **E1** | ① 其他转休(MANUAL) 之间，同人同日**仅允许一条**；MANUAL 与加班转休**互不干扰** |
| **E2** | **需要判重，且跨模块**：加班转休 ↔ 调休使用 也纳入**同一套区间重叠判定**（注意：不是"同日仅一条"，而是按区间） |
| **E3** | 端点相接**不算**重叠（15:00–17:00 与 17:00–19:00 允许并存） |

### 最终生效的规则集（按记录分组）

设某员工 P 在某日 D 的**有效记录（未作废）**分为三组：

| 组 | 来源 | 是否带起止时间 | 约束 |
|---|---|---|---|
| **G1** | `overtime_records`，`record_mode = 'OVERTIME'` | 是 | **G1 ∪ G2 内两两区间不得重叠** |
| **G2** | `leave_usage_records` | 是 | 同上（与 G1 合并判定，即**跨模块**） |
| **G3** | `overtime_records`，`record_mode = 'MANUAL'` | **否** | 同人同日**最多一条**；与 G1/G2 **互不干扰** |

判定式（半开区间，端点相接不算重叠）：`重叠 ⇔ A.start < B.end && B.start < A.end`

### ⚠️ 一个由表结构决定、必须记录的边界

**「其他转休」(G3) 表结构上就没有起止时间**——`OvertimeService` 第 483–484 行显式 `setStartTime(null)` / `setEndTime(null)`，实体注释也写明"其他转休为空"。因此它**无法参与区间重叠判定**，只能按 E1① 退化为"同人同日最多一条"。

这是**表结构决定的，不是设计取舍**。若将来希望 G3 也纳入区间判重，唯一办法是给「其他转休」补填起止时间（即原 E1 的选项③）——**否则这条规则永远管不到它**。

## 2.3 实现要点

1. **查询**：新增 `findByUserIdAndDateAndStatusNot(userId, date, 'VOID')`，把当日有效记录一次拉出，在内存里逐条跑上面的判定式。比在 SQL 里写区间条件清晰得多。
2. **作废记录不参与判重**：已作废(VOID)的记录不应阻塞新录入（加班现有查重已排除 VOID，保持一致）。
3. **入口要挂全**：`OvertimeService.createOne`、`OvertimeService.update`（排除自身）、`LeaveService.createOne`、`LeaveService.update`（排除自身）、`LeaveService.createBatch`（还需**批内两两**判定，因为批内多条在保存前彼此不可见）。
4. **跨模块查询**：一次 `assertIntervalFree` 同时拉 G1（加班转休）与 G2（调休使用）两表，合并判定——这正是 E2 的要求。
5. **数据库兜底的真实局限**：MySQL **没有区间排他约束**（PostgreSQL 的 `EXCLUDE` 做不到）。所以唯一索引只能覆盖"完全相同的 (userId, date, startTime)"这一种情形，**覆盖不了包含与交叉**。真正的防重必须靠应用层 + 写锁串行。这一点必须写进文档，别指望数据库能兜住。

## 2.4 与 P0-2 / D1 的强耦合（重要）

现在"先提交的赢"**是靠 `WriteGate` 全局串行实现的**，不是专门设计。所以：

- 如果保持全局写锁 → 判重安全，但第二个人要等 3 秒；
- 如果改成"按用户分片锁"（`修复方案.md` 的 D1-B）→ **分片键必须包含被录入的 `userId`**，否则并发窗口会重新打开，判重就又漏了；
- **这两件事必须一起设计**，不能分开做。好消息是分片锁只要键选对，既保安全又能让不同员工真正并行。

## 2.5 历史数据处理

上线判重后，**已经存在的重叠记录会导致"无法编辑该记录"**（一保存就报重复）。建议上线前先跑一次盘点脚本，把现存重叠记录列出来人工确认，再决定是保留还是清理。

## 2.6 实现落地记录（本次已改代码）

按 E1/E2/E3 的最终口径，判重已落到代码（后端编译通过）：

| 新增/改动 | 说明 |
|---|---|
| **新增 `RecordConflictGuard`** | 判重守卫，全系统唯一的判重出口。`assertIntervalFree()` 一次拉 G1+G2 两表合并判定；`assertManualOnce()` 处理 G3 同日仅一条；`overlaps()` 实现半开区间判定式 |
| `OvertimeRepository` | 删除 `findOverlappingStart`（只比相同起始时间，已被取代），新增 `findEffectiveByUserIdAndDate` |
| `LeaveUsageRepository` | 新增 `findEffectiveByUserIdAndDate`（原来**完全没有**判重查询） |
| `OvertimeService.createOne` | 替换旧口径。这是**所有加班录入的唯一漏斗**（单条 `create`、批量 `createBatch`、Excel 导入都走它），一处改动全覆盖 |
| `OvertimeService.update` | 改为区间判重，并排除自身记录 |
| `LeaveService.createBatch` | 新增**批内两两**判定（批内记录保存前彼此不可见）+ 逐条对库判定 |
| `LeaveService.createOne` | 新增区间判重（Excel 导入逐行复用它，故导入同样生效） |
| `LeaveService.update` | 新增区间判重，排除自身记录 |

**并发安全依据**：`WriteGuardInterceptor` 对所有 POST/PUT/DELETE 调用 `WriteGate.acquire()` 全局串行，且锁在**事务提交之后**（`afterCompletion`）才释放——因此"先查后写"不存在竞态，天然满足"先提交的赢"。

**提示文案**：已统一为「该员工在 {日期} 已存在时间重叠的记录（{明细}）。该数据可能已存在，也可能由他人刚刚提交，请刷新后重试」，并按 P1-6 保留了员工/日期/时间定位信息。

---

# 第三部分 · 日历双状态重构 · 可行性研判

## 3.1 现状

| 维度 | 现状 |
|---|---|
| 存储 | `holidays` 表，一天一行；`type` 三值：`LEGAL` / `WORKDAY` / `RESTDAY`；另有 `officialType`（官方原始值）与 `auto`（是否系统生成） |
| 衍生状态 | `ADJUSTED`（调休补班）**不存库**，由 `determineDayType()` 用「类型=工作日 **且** 当天是周末」推断出来 |
| 判定优先级 | 人工维护 > 内置官方规则 > 周末默认 > 工作日默认 |
| 前端 | `Holiday.vue` 三类型下拉（含配色），年度概览统计 `legalCount`/`workdayCount`/`restCount`，"高风险变更"判定基于「是否改变 LEGAL 属性」 |

## 3.2 核心发现：系数硬编码在 3 个地方

这是本次重构**唯一真正的障碍**，也是"未来适配性"问题的根源：

| # | 位置 | 代码 |
|---|---|---|
| 1 | `ChineseHolidayRules.ratioOf(type)` (213) | `工作日 → 0.5`，其余 `→ 1` |
| 2 | `HolidayService.ratioForDayType(dayType)` (404) | `工作日/补班日 → 0.5`，其余 `→ 1` |
| 3 | `OvertimeService.ratioOf(dayType)` (320) | `工作日/ADJUSTED → 0.5`，其余 `→ 1` |

三份逻辑各自独立。**只要改系数，必须三处同改，漏一处就会出现"日历显示 A、计算按旧逻辑"的静默不一致。**

## 3.3 可行性结论：✅ 可行，且是一次正向重构

**关键论证：为什么业务结果不会变。**

当前 `LEGAL` 与 `RESTDAY` 的系数**都是 1**（见 3.2 三处实现）。所以把它们合并为 `B（法定休息日）`：

| 原类型 | 系数 | 新 dayKind | 系数 |
|---|---|---|---|
| `WORKDAY`（含补班） | 0.5 | **A 法定工作日** | 0.5 |
| `RESTDAY` | 1 | **B 法定休息日** | 1 |
| `LEGAL` | 1 | **B 法定休息日** | 1 |

**合并后每一条日期的折算系数完全不变 → 所有历史加班记录换算结果不变 → 业务零影响。**

而且顺带得到三个**正向收益**：

1. **`ADJUSTED` 这个衍生类型可以消失**。现在要靠"类型=工作日且是周末"来推断补班，绕；双状态下补班日就是 A，直截了当。判定链路从 4 态降到 2 态。
2. **"高风险变更"判定会变准确**。现在的高风险判定看"是否改变 LEGAL 属性"，但真正影响金额的是**系数变化**（A↔B）。改成双状态后，"改 A/B 即高风险"才真正对上了钱。
3. **与 P2-4 合流**：把系数从硬编码抽成可配置映射，**同时解决**审计里"系统配置页形同虚设"的发现。

## 3.4 ⚠️ 必须知道的一个取舍

`LEGAL` 消失后，**"法定节假日"与"普通周末"在类型维度上不再区分**。

当前系统两者系数都是 1，所以**没问题**。但如果将来需求变成：

> 法定节假日加班算 3 倍、周末加班算 2 倍（劳动法就是这么规定的）

那么 A/B 双状态**不够用**，需要引入第三态。

**这不构成反对理由**——只要按 3.5 的设计做，将来加第三态（C）时映射表结构不用动。但你现在应该知道这个边界在哪。

## 3.5 推荐设计（面向未来的三层结构）

### 第一层：日历表 —— 只存"日类型"

```java
// Holiday.java
public static final String KIND_WORKDAY = "A";   // 法定工作日（含调休补班日）
public static final String KIND_RESTDAY = "B";   // 法定休息日（含法定节假日、普通周末）

@Column(nullable = false, length = 1)
private String dayKind = KIND_WORKDAY;           // 用 A/B 存储，预留 C、D 扩展位
```

同时把 `officialType` 一并改为 `officialDayKind`，「人工改动识别」逻辑同步调整。

> **为什么用 A/B 而不是 `WORKDAY`/`RESTDAY` 字符串**：你明确要的就是 A/B；单字符编码在数据表里更省、更稳定，且**语义由代码常量承担**（`DayKind.A` 配注释），不靠字符串本身自解释。这是紧凑编码 + 语义常量的常见做法。

### 第二层（新增）：系数映射表 —— 只存"规则"

```sql
CREATE TABLE overtime_ratio (
  id          BIGINT PRIMARY KEY AUTO_INCREMENT,
  work_type   VARCHAR(30) NOT NULL,   -- 加班类型，初期只有 'DEFAULT'
  day_kind    CHAR(1)     NOT NULL,   -- 'A' / 'B'（未来可 C）
  ratio       DECIMAL(4,2) NOT NULL,  -- 折算系数
  updated_at  DATETIME,
  UNIQUE KEY uk_type_kind (work_type, day_kind)
);
-- 初始数据
-- ('DEFAULT','A',0.50)
-- ('DEFAULT','B',1.00)
```

**这一层就是"未来适配性"的答案**：加班类型变多了 → 加 `work_type` 行；系数调整了 → 改 `ratio` 值；将来要区分法定节假日 → `day_kind` 加 `C`。**表结构永远不用改。**

### 第三层：统一出口 —— 消灭三处硬编码

```java
// HolidayService：全系统唯一的系数来源
public BigDecimal ratioOf(String date) {                 // 传入日期，内部判类型再查表
    String kind = determineDayKind(date);
    return ratioRepository.find("DEFAULT", kind)
                          .map(OvertimeRatio::getRatio)
                          .orElseThrow(() -> new BizException("未配置系数：" + kind));
}
```

然后 `OvertimeService`、`ChineseHolidayRules.ratioOf`、前端 `/api/holidays/resolve` 全部改为调用这一个出口。**3 处硬编码 → 1 处查表。**

## 3.6 调休录入的休息日提示（P5-3）

现状：调休录入**完全不判断日类型**（已核实 `LeaveService` 无任何相关调用）。

建议流程：

```
1. 提交调休 → 后端 determineDayKind(date)
2. 若为 B（休息日）且请求未带 confirmRestDay=true
   → 返回 BizException(code=4091, "该日期是休息日，通常无需使用调休，确认要继续吗？")
3. 前端捕获 4091 → 弹二次确认
4. 用户点"确认" → 带 confirmRestDay=true 重发 → 放行
```

注意这是「**前端二次确认 + 后端放行**」，与 P1-1 的"强制改密"不同——后者是硬拦截，这里是软提醒。两者不要混用同一套机制。

## 3.7 迁移方案（推荐三阶段，可回滚）

| 阶段 | 动作 | 风险 | 可回滚 |
|---|---|---|---|
| **阶段 1** | 新增 `day_kind` 字段与 `overtime_ratio` 表；**双写**（`type` 与 `day_kind` 同时维护）；**业务逻辑仍读旧字段** | 极低 | 是（纯新增） |
| **阶段 2** | 切换读取到 `day_kind` + 映射表；`type` 继续双写保留 | 中（需验证系数一致） | 是（改回读旧字段） |
| **阶段 3** | 下线 `type` 字段、`ADJUSTED` 衍生逻辑；前端改双状态 UI | 低 | 需备份 |

数据迁移映射（一次性 UPDATE，一年仅约 365 行，风险极低）：

```sql
UPDATE holidays SET day_kind = CASE type
  WHEN 'WORKDAY' THEN 'A'
  ELSE 'B'                    -- LEGAL 与 RESTDAY 均归为 B
END;
```

## 3.8 风险清单

| 风险 | 等级 | 说明 | 对策 |
|---|---|---|---|
| 三处系数硬编码漏改 | **高** | 出现"界面显示 A、计算按旧逻辑"的静默不一致，且不报错 | 阶段 2 切换时加一致性校验：同一日期分别用新旧路径算，比对结果 |
| `OvertimeRecord.dayType` 历史数据 | 中 | 历史加班记录存的是 `WORKDAY`/`RESTDAY`/`LEGAL`/`ADJUSTED` | **不动历史字段**，只在新逻辑用新编码，展示时映射（`OvertimeService.dayTypeLabel` 已有兼容分支） |
| 前端三类型 UI 与统计 | 中 | `Holiday.vue` 有配色、下拉、`legalCount`/`workdayCount`/`restCount` 三组统计 | 同步改为双状态；原 `legalCount` 可用 `name != null` 近似表达"法定节假日" |
| 「高风险变更」判定依据改变 | 低 | 判定基准从"是否 LEGAL"改为"是否改变 A/B" | 这是 **正向变化**，见 3.3 |
| 人工改动识别(`isManualChanged`) | 中 | 依赖 `officialType` 与 `type` 比对，编码变了要同步 | 阶段 1 一并迁移 |

## 3.9 结论与建议

**结论：该重构可行，风险可控，且顺带解决两个已有遗留问题（配置页形同虚设、补班概念绕）。**

**但我建议不要现在立刻做**，理由是它与 P1/P2 有耦合：

1. **P5-2（系数抽表）与 P2-4（配置页定去向）是同一件事** —— 应该合并做一次，而不是分两次改同一块代码。
2. **P5-3（调休休息日提示）与 P1-3（调休判重）都改 `LeaveService.createOne`** —— 应该合并在同一次改动里，避免二次返工。
3. **P5 依赖 P0-2 / D1 的写锁决策** —— ✅ 该决策已定并于 2026-09-11 上线（三层方案），P5 的写路径前提已就绪。

**推荐顺序**：`P0 → P1 → P2 → P4 → P5`，其中 P5 内部按 3.7 的三阶段走。

---

# 第四部分 · 需要你拍板的事项汇总

| 编号 | 事项 | 选项 | 结论 |
|---|---|---|---|
| **D1** ✅ | `WriteGate` 走向 | A 保留全局串行（有界等待）／ B 按用户分片锁 | ✅ **已上线（2026-09-11）**：三层 = ① DB `active_key` 唯一索引兜"完全相同" ② 删 3s 冷却 + `tryLock(3s)`→429 ③ 保留程序判重。**暂不做 B（按用户分片）** |
| **D2** ✅ | 系统配置页 | A 接上业务／ B 整体下线 | **A 接上业务**（与 P5-2 合并做） |
| **D3** ✅ | 登出语义 | A 全设备下线／ B 只退当前设备（黑名单） | **B 令牌黑名单**（只作废当前令牌，不影响其他设备） |
| **B1** ✅ | 内置管理员默认密码 | 配置文件／ 环境变量 | **环境变量**（与 P0-1 同批） |
| **B2** ✅ | 内置管理员是否豁免登录锁定 | 豁免／ 保留 | **豁免**，但必须同时补 P2-1 的 IP 限流 |
| **B3** ✅ | 系统日志是否隐藏 admin 操作 | 保留／ 隐藏 | **保留** |
| **B4** ✅ | 管理员"不显示"方式 | A 纯隐藏／ B 显示但只读／ C 隐藏+页顶提示 | **A 纯隐藏**（补救：创建用户时若命中被隐藏的内置账号，给出专门提示，避免"用户名已存在却看不到"的困惑） |

> **D1 现状事实（已核对代码）**：`WriteGate` 是单例里的**一把全局 ReentrantLock**，`WriteGuardInterceptor` 对 `/api/**` 下所有 POST/PUT/DELETE 生效；`acquire()` 抢锁后还会等到"距上次 `release()` 满 `write-cooldown-ms`(默认 3000)"。因此**全系统写吞吐上限 ≈ 20 次/分钟，与数据是否相同无关**。锁本身只持几毫秒，**3 秒冷却是"全场干等"的真正原因**。
| **E1** ✅ | **其他转休与加班转休是否互斥** | ①互不干扰 ②同日仅一条 ③补填时间 | **①**（已定） |
| **E2** ✅ | **加班转休与调休使用是否跨模块判重** | 判重／ 不判重 | **判重，且按区间重叠**（已定，推翻原"不判重"） |
| **E3** ✅ | **时间区间端点相接是否算重复** | 算／ 不算 | **不算**（已定，15–17 与 17–19 允许） |
| **E4** ✅ | **日历重构是否现在做** | 立即／ 按 P0→P5 顺序 | **按顺序**（已定），与 P2-4、P1-3 合并做 |

E1–E4 已全部锁定，判重规则（第二部分）已落地实施。**D1 / D2 / D3 / B1–B4 决策已全部锁定**；其中 **D1 三层方案 + P0-1/P0-2/P0-3/P1-7 已于 2026-09-11 上线**，**D2 / D3 / B2（补 IP 限流）/ B4（列表隐藏）尚待实施**。
