# 调休管家 · 手工测试用例集（全流程）

> 对应本轮改动：`7199ed4`（安全加固/内置管理员/令牌黑名单/日历双态/判重守卫/前端配套）
> + `cefb158`（客户端错误不再误报 500、构建不残留旧分片）
> + `5d87583`（删除折算系数死代码）
>
> 编写依据：逐条核对源码（后端 `server-java/src/main/java/com/tiaoxiu`，前端 `web/src`），
> 预期结果中的提示文案均为**可粘贴的原文**。

---

## 0. 测试前必读

### 0.1 响应形态（最关键，断言别只看 HTTP 状态）

后端大部分业务拒绝**不改 HTTP 状态**，约定如下：

| 形态 | HTTP | body | 触发场景 |
|---|---|---|---|
| 业务成功 | 200 | `{"code":0,"message":"ok","data":…}` | 正常流程 |
| 业务拒绝 | **200** | `{"code":1,"message":"<原文>"}` | 校验失败、余额不足、判重命中、内置管理员保护… |
| 鉴权失败 | **401** | `{"code":401,"message":"<原文>"}` | 无 token / token 失效 / 被拉黑 / 账号冻结 |
| 越权 | **403** | 强制改密期越权、Swagger 非 ADMIN | — |
| 写锁超时 | **429** | `{"code":429,"message":"系统繁忙，请稍后重试"}` | 并发写 |
| 路径不存在 | **404** | `{"code":404,"message":"请求的接口不存在"}` | 本次修复 |
| 方法不支持 | **405** | `{"code":405,"message":"请求方式不支持，该接口仅允许 [POST]"}` | 本次修复 |
| 请求体坏 | **400** | `{"code":400,"message":"请求数据格式错误，请检查提交内容"}` | 本次修复 |
| 未预期异常 | **500** | `{"code":500,"message":"服务器内部错误，请稍后重试（错误编号 <12位>）"}` | 脱敏，不含堆栈 |

**所以「预期结果」要写「HTTP 200 + code=1 + message=…」，不能写「返回 400」。**

### 0.2 环境与账号

- 后端：`http://localhost:8600`（`start.bat` 启动；需 MySQL 3306）
- 前端：随 jar 发布，直接访问 `http://localhost:8600`
- 账号口令：取自仓库根目录 `env.local.bat`（`DB_*` / `JWT_SECRET` / `ADMIN_DEFAULT_PASSWORD`），
  代码内默认口令 `app.default-password = Abc_123456`
- 建议准备 3 个账号：`admin`（内置管理员）、一个 ADMIN 角色自建号、一个 EMPLOYEE 号（新建后首次登录须改密）

### 0.3 关键配置基线（用于核对环境是否被改过）

| 配置项 | 基线值 | 说明 |
|---|---|---|
| `app.login-lock-threshold` | 5 | 连续失败 5 次锁定，**仅对非 ADMIN 生效** |
| `app.login-lock-minutes` | 15 | 锁定时长 |
| `app.login-ip-limit` | 30 | 同 IP 窗口内尝试上限 |
| `app.login-ip-window-seconds` | 300 | 窗口 5 分钟 |
| `app.write-lock-timeout-ms` | 3000 | 写锁抢锁超时 → 429 |
| `app.upload.allowed-extensions` | xlsx,xls | 导入白名单 |
| `app.upload.max-rows` | 5000 | 单次导入行数上限 |
| `app.cors-allowed-origins` | `http://localhost:8601,http://127.0.0.1:8601` | CORS 白名单 |
| `leave.ratio.workday` | 0.5 | 【生效中】法定工作日/补班日折算比例 |
| `leave.ratio.rest` | 1 | 【生效中】法定休息日（含法定节假日）折算比例 |

### 0.4 2026 年日历基线数据（真实官方规则，可直接当断言用）

| 日期 | 星期 | 预期 dayType | 类型名 | 系数 | holidayName |
|---|---|---|---|---|---|
| 2026-09-15 | 周二 | `WORKDAY` | 法定工作日 | 0.5 | null |
| 2026-09-19 | 周六 | `RESTDAY` | 法定休息日 | 1 | null |
| 2026-10-01 | 周四（国庆） | `RESTDAY` | 法定休息日 | 1 | 国庆节 |
| 2026-10-10 | 周六（补班） | `WORKDAY` | 法定工作日 | 0.5 | 调休补班 |
| 2026-02-15 | 周日（补班） | `WORKDAY` | 法定工作日 | 0.5 | 调休补班 |

> ⚠️ **补班日不会返回 `ADJUSTED`**。`ADJUSTED` 仅由死代码 `HolidayService.determineDayType` 推导，
> resolve 流程不走它。断言补班日请写 `dayType=WORKDAY` + `holidayName=调休补班`。

---

## A. 部署与构建验收（本次发布）

| 编号 | 用例 | 步骤 | 预期结果 |
|---|---|---|---|
| A-01 | 服务启动 | 执行 `start.bat` | 日志出现 `Started TiaoxiuApplication`；`SecurityStartupValidator` 输出「启动期安全基线校验通过：凭据均来自环境变量，jwt.secret 长度 64 字符」；Tomcat 监听 8600 |
| A-02 | 表结构校验 | 观察启动日志 | `ddl-auto: validate` 通过，无实体与表不匹配报错 |
| A-03 | 缺环境变量拒绝启动 | 不加载 `env.local.bat` 直接跑 jar | **拒绝启动**（`SecurityStartupValidator` 故意设计）；用占位 JWT 密钥同理 |
| A-04 | 健康检查 | `GET /api/auth/health` | 200，`{"code":0,"message":"ok","data":"ok"}`（无需 token） |
| A-05 | 首页可用 | 浏览器访问 `http://localhost:8600` | 正常出登录页；页面引用的 `assets/index-*.js` 实际可下载（200） |
| A-06 | 产物无旧分片 | 检查 `server-java/src/main/resources/static/assets/` | 每个页面 chunk **只有一份**（无 `Overtime-*.js` 多版本残留） |
| A-07 | 启动日志无假错误 | 启动后 `grep " ERROR "` 日志 | **0 条** ERROR |

---

## B. 登录与会话安全

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| B-01 | — | 正确账号 + 正确口令登录 | HTTP 200 + code 0，data 含 token、用户信息、菜单；前端提示「欢迎回来，<姓名>」 |
| B-02 | — | 正确账号 + **错误口令** | HTTP **200** + code 1 + message `用户名或密码错误` |
| B-03 | — | **不存在的账号** + 任意口令 | HTTP **200** + code 1 + message `用户名或密码错误`（与 B-02 **文案完全一致**，防账号枚举） |
| B-04 | 非 ADMIN 账号 | 连续输错 5 次 | 第 5 次起 message `密码错误次数过多，账号已锁定 15 分钟` |
| B-05 | B-04 后 | 锁定期内用**正确口令**登录 | HTTP 200 + code 1 + message `账号已锁定，请于 X 分钟后重试`（X 为剩余整数分钟，最小 1） |
| B-06 | B-04 后 | 等待 15 分钟后再登录 | 恢复正常；且成功登录会把失败计数清零 |
| B-07 | `admin`（ADMIN） | 连续输错 5 次以上 | **不触发锁定**（锁定仅对非 ADMIN 生效） |
| B-08 | — | 同一 IP 在 5 分钟内发起 >30 次登录 | message `登录尝试过于频繁，请稍后再试`，并产生一条 WARN 审计日志 |
| B-09 | — | 不带 `Authorization` 访问任意 `/api/*` 受保护接口 | HTTP **401** + message `缺少令牌` |
| B-10 | — | 带伪造 / 语法错误的 token | HTTP **401** + message `令牌无效或已过期` |
| B-11 | — | 不带 token 访问一个**不存在**的 `/api/xxx` | HTTP **401**（先鉴权，**不泄露路径是否存在**） |
| B-12 | 持有效 token | 带 token 访问不存在的 `/api/xxx` | HTTP **404** + code 404 + `请求的接口不存在` |

---

## C. 强制改密与密码策略

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| C-01 | 管理员新建一个用户，口令留空 | 用该账号登录 | 登录返回 `mustChangePwd=true`；前端 `replace` 跳转改密页（不可用浏览器返回到业务页） |
| C-02 | 处于强制改密状态 | 直接调任意业务接口（如 `GET /api/holidays/resolve`） | HTTP **403** + `请先修改初始密码后再继续操作` |
| C-03 | 同上 | 调 `/api/auth/me`、`/api/auth/logout`、`/api/auth/change-password` | **允许**（白名单内），不返回 403 |
| C-04 | 同上 | 手输地址绕到 `#/dashboard` | 页面骨架能渲染，但数据接口被 403 拦掉并弹错误提示——**强制逻辑以后端为准，前端跳转不是唯一防线** |
| C-05 | 改密页 | 输入新口令 `abc`（过短） | 前端拦：`新密码需 8-20 位，且至少包含字母与数字` |
| C-06 | 改密页 | 新口令 `abcdefgh`（纯字母，无数字） | 同 C-05 文案 |
| C-07 | 改密页 | 新口令与原口令相同 | `新密码不能与原密码相同` |
| C-08 | 改密页 | 两次新口令不一致 | `两次输入的新密码不一致` |
| C-09 | 改密页 | 原密码填错 | 后端 message `原密码错误`（HTTP 200 + code 1） |
| C-10 | — | 新口令 `abc12345`（字母+数字，8 位） | 通过（低强度）；界面强度灯颜色：不符合=灰 / 低=红 / 中=黄 / 高=绿 |
| C-11 | 改密成功 | 观察返回与旧 token | 返回**新 token**，`mustChangePwd` 置 false；**旧 token 立即失效**（再用即 401 `登录状态已失效，请重新登录`）；前端提示「密码修改成功，请重新登录」并回登录页 |
| C-12 | **`admin`（内置管理员）** | 用 admin 登录 | **不触发强制改密**（builtin 的 `mustChangePwd` 恒为 false） |
| C-13 | `admin` | 进入改密页提交 | 后端拒绝：`内置管理员的密码不可通过界面修改。如需变更，请由运维设置环境变量 ADMIN_DEFAULT_PASSWORD 后重启服务`（前端**未**禁用该入口，属预期行为） |

---

## D. 登出与令牌黑名单

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| D-01 | 同一账号在**两台设备**（两个浏览器）登录，各持 token A / B | 设备 A 点退出登录 | `POST /api/auth/logout` 返回 200 code 0 |
| D-02 | D-01 后 | 用 token A 请求受保护接口 | HTTP **401** + `登录状态已失效，请重新登录` |
| D-03 | D-01 后 | 用 token B 请求受保护接口 | **正常可用**（单设备登出，不影响其它设备——多设备共存是明确需求） |
| D-04 | D-01 后 | 查 `token_blacklist` 表 | 新增一条：`token_hash` 为 64 位 SHA-256（**不是明文 token**）、`user_id`、`expires_at`、`created_at` |
| D-05 | 造一条 `expires_at` 已过期的黑名单记录 | 观察每日 03:10 的清理任务 | 过期记录被自动删除（cron `0 10 3 * * ?`） |
| D-06 | — | 退出登录后**重启后端** | 黑名单仍生效（落库持久化，非内存） |

---

## E. 权限与菜单

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| E-01 | `admin` 登录 | 查看侧边菜单 | 菜单由后端 `/api/auth/me` 的 `menus` 下发，前端不写死；`/system/role`、`/system/resource`、`/system/config` **不出现在菜单**，只能手输地址进入 |
| E-02 | EMPLOYEE 账号登录 | 查看菜单 | 仅本人相关页（工作台/加班/调休/日历/全员查看/统计/个人中心），无用户管理、角色、资源、配置、日志 |
| E-03 | EMPLOYEE | 手输 `#/user`（无权限） | **重定向到 `#/dashboard`**（系统**没有 403 页面**，也没有空白页）；输任意乱码路径同样兜底到 `#/dashboard` |
| E-04 | EMPLOYEE | 在加班页观察操作按钮 | `导入` / `导出` / `＋增加时长` 按钮**从 DOM 中移除**（`v-permission` 是移除而非禁用） |
| E-05 | EMPLOYEE | 直调 `POST /api/overtime/batch` | HTTP 200 + code 1 + `无权限：仅管理员/录入员可录入`（角色级强校验，前端隐藏只是体验） |
| E-06 | 非 ADMIN | 调 `GET /api/system-logs` | HTTP 200 + code 1 + `无权限：仅管理员可查看系统日志` |
| E-07 | 非 ADMIN | 调 `POST /api/config` | HTTP 200 + code 1 + `无权限：仅管理员可配置` |

---

## F. 内置管理员与用户管理

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| F-01 | `admin` | 打开用户管理列表 | **列表中没有 `admin` 这一行**（后端 `filter(!builtin)` 过滤，非前端隐藏） |
| F-02 | 已知 adminId | 调 `DELETE /api/users/{adminId}` | `内置管理员不可删除`（HTTP 200 + code 1） |
| F-03 | 管理员自己 | 删除**自己**的账号 | `不能删除自己，请由其他管理员操作` |
| F-04 | 已知 adminId | 停用/冻结 `admin` | `内置管理员不可被冻结` |
| F-05 | 管理员自己 | 冻结/启用自己 | `不能冻结或启用自己，请由其他管理员操作` |
| F-06 | 已知 adminId | 把 `admin` 的角色改成非 ADMIN | `内置管理员的角色不可修改` |
| F-07 | 已知 adminId | 重置 `admin` 的口令 | `内置管理员的密码不可重置。如需变更，请由运维设置环境变量 ADMIN_DEFAULT_PASSWORD 后重启服务` |
| F-08 | — | 新建用户名填 `admin` | `该用户名为系统内置账号保留，不可重复创建`；换一个已存在的普通用户名则提示 `用户名已存在` |
| F-09 | — | 新增用户，口令留空 | 采用默认口令（`app.default-password`，基线 `Abc_123456`），且 `mustChangePwd=true` |
| F-10 | — | 新增用户，口令填 `abc` | 前端/后端拦截：`密码强度不足：密码需 8-20 位，且至少包含字母与数字（同时含大小写字母为中强度，再加特殊字符为高强度）` |
| F-11 | — | 新增用户，必填项留空 | `姓名、账号、部门、职位、角色均为必填项` |
| F-12 | 目标用户在线 | 管理员**重置**该用户口令 | 提示 `密码已重置，对方下次登录需改密`；目标用户 `tokenVersion+1`，其**当前会话下一次请求即 401** `登录状态已失效，请重新登录`；重新登录后强制改密 |
| F-13 | 目标用户在线 | 管理员**冻结**该用户 | 目标用户会话立即失效（401 `登录状态已失效，请重新登录`），且无法再登录（`账号已被冻结或删除`） |
| F-14 | 目标用户在线 | 管理员**修改目标用户角色** | 角色集合变化会 `tokenVersion+1` → 目标需**重新登录**后新权限/菜单才生效 |
| F-15 | — | 改角色后不重新登录，继续用旧 token | 401 `登录状态已失效，请重新登录` |
| F-16 | — | 管理员修改**自己**的资料 / 重置自己的口令 | 不做 `requireNotSelf` 拦截（编辑自己、重置自己口令属允许操作，仅内置管理员被额外保护） |

---

## G. 日历与折算系数

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| G-01 | — | `GET /api/holidays/resolve?date=2026-09-15` | code 0，`dayType=WORKDAY`、`typeLabel=法定工作日`、`ratio=0.5`、`holidayName=null` |
| G-02 | — | `…?date=2026-10-01` | `dayType=RESTDAY`、`typeLabel=法定休息日`、`ratio=1`、`holidayName=国庆节` |
| G-03 | — | `…?date=2026-10-10`（周六补班） | `dayType=WORKDAY`、`ratio=0.5`、`holidayName=调休补班`（**不是 ADJUSTED**） |
| G-04 | — | `…?date=2026-09-19`（周六） | `dayType=RESTDAY`、`ratio=1`、`holidayName=null` |
| G-05 | — | 同一日期分别调 `/api/holidays/resolve` 与 `/api/overtime/resolve` | 两接口**同源**：`type==dayType`、`ratio` 一致、`name==holidayName` |
| G-06 | — | 观察年度概览 `GET /api/holidays/year?year=2026` | 每月仅含 `workdayCount` / `restCount` / `manualCount`——**已无 `legalCount`**（三态并双态） |
| G-07 | — | 打开日历页月份卡片 | 仅**两格**：法定工作日 / 法定休息日 + 「已人工调整 N 天」——**已删「法定节假日」格** |
| G-08 | — | 单天编辑下拉框 | 类型选项只有 **法定工作日 / 法定休息日** 两项（LEGAL 已删） |
| G-09 | — | 把某普通周六（无节假日名）改为工作日 | 直接保存成功，提示 `已保存 N 天变更`，**不触发**高风险确认 |
| G-10 | — | 把 **2026-10-01（国庆）** 的类型改掉 | 弹「高风险操作确认」，正文含 `该日期为国家法定节假日「国庆节」，变更可能影响历史数据与后续加班折算口径，是否继续？`；按钮 确认变更 / 取消 |
| G-11 | 人工把某天改为与官方规则不同 | 再查该天 resolve | 返回人工维护的值，且 `manual=true`（优先级：人工 > 官方规则 > 周末兜底） |
| G-12 | 已有人工变更 | 点「刷新」 | 弹「刷新冲突确认」：`以下 N 天存在人工变更：… 是否强制刷新并覆盖？`，按钮 强制刷新 / 取消；不点强制则不写库 |
| G-13 | — | 把 `leave.ratio.workday` 改成 `0.8` 保存 | 提示 `配置已保存`；**无需重启**，立即 `GET /api/holidays/resolve?date=2026-09-15` 返回 `ratio=0.8`（内存缓存 set 后置空） |
| G-14 | G-13 后 | 恢复为 `0.5` 并复查 | 重新返回 `0.5`（**用例结束务必复原**） |
| G-15 | — | 查看配置页 `overtime.dailyCap`、`overtime.period.am/pm` | 说明列以「【已废弃·不生效】」开头并带灰色「已废弃」标签；值仍可编辑，但**业务不读取**（改它加班时长不会封顶、不按 09:00-12:30 扣减） |
| G-16 | 库中残留历史 `LEGAL` 值的日期 | 查该天 resolve / 页面 | 归一为 RESTDAY，显示「法定休息日」，系数走 `ratioRest()`，**无报错** |

---

## H. 加班录入

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| H-01 | 加班转休模式 | 日期选 2026-09-15（工作日） | 类型框显示「法定工作日」，比例框显示 **0.5** |
| H-02 | 加班转休模式 | 日期选 2026-10-01（国庆） | 类型框显示「法定休息日·国庆节」，比例框显示 **1.0** |
| H-03 | — | 填 09:00–12:00，打卡时间已填，提交 | 加班时长 3.00；转休时长 = 3 × 系数，保留 2 位四舍五入；提示 `已录入 N 条记录` |
| H-04 | 加班转休 | 开始时间填 `09:15` | 拒绝：`开始时间须为整点或半点（分钟为 00 或 30）` |
| H-05 | 加班转休 | 结束时间填 `12:20` | 拒绝：`结束时间须为整点或半点（分钟为 00 或 30）` |
| H-06 | 加班转休 | 结束时间早于开始时间 | 前端 `结束时间必须晚于开始时间`；后端 `结束时间必须晚于开始时间` |
| H-07 | 加班转休 | **不填打卡时间**提交 | `请填写打卡时间`（加班转休模式打卡时间必填，用于与考勤核对） |
| H-08 | 加班转休 | 不选开始/结束时间 | `请选择开始时间` / `请选择结束时间` |
| H-09 | 其他转休模式 | 只填 姓名 + 日期 + 转休时长，提交 | 成功后 **`ratio` 落库为 null**（其他转休不算系数，按输入值直接入账） |
| H-10 | 其他转休 | 转休时长填负数 | `转休时长不能为负数` |
| H-11 | 其他转休 | 转休时长留空 | `请填写转休时长` |
| H-12 | 其他转休 | **通过界面**不填备注提交 | 前端拦：`第 N 行：其他转休的备注为必填项` |
| H-13 | 其他转休 | **直接调接口**不带 `remark` | ⚠️ **会成功**（后端无该校验）——见「已知不一致」章节，需确认口径 |
| H-14 | — | 不选员工提交 | `请选择员工姓名` |
| H-15 | — | 删除一条已有记录 | 弹「删除确认」：`确定删除 <姓名> <日期> 的记录吗？已折算的 Xh 调休将一并冲减。`，按钮 确定删除 / 取消 |

---

## I. 调休使用（Leave）

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| I-01 | — | 录入弹窗内查看余额 | 每行下方显示「剩余余额 X.XX 小时」，负数标红 |
| I-02 | 余额 8h | 使用 10h（未勾选允许欠休） | 返回 `余额不足，当前余额 8.00 小时，本次使用 10.00 小时，是否继续？`；前端弹「余额不足」确认框（确认继续 / 取消） |
| I-03 | I-02 | 点「确认继续」 | 带 `allowOverdraft=true` 重提 → 成功，透支入账并生成余额预警提醒 |
| I-04 | — | 使用日期选 2026-09-19（周六） | 返回 `所选日期属于休息日：2026-09-19。休息日本不用上班，在此登记调休会直接扣减余额，是否继续？` |
| I-05 | I-04 | 点「确认继续」 | 带 `allowRestDay=true` 重提 → 成功 |
| I-06 | — | 日期选 2026-10-01（国庆） | 文案中日期后**附带节假日名**：`所选日期属于休息日：2026-10-01（国庆节）。…是否继续？` |
| I-07 | 员工余额不足 | 点「取消」 | 不提交，余额与记录均不变 |
| I-08 | — | 批量录入中同一人同一天填两条重叠时段 | `本次提交中存在同一天时间重叠的记录：<date> <start>–<end> 与 <start>–<end>，请调整后重试` |
| I-09 | — | 作废一条调休使用记录 | 弹确认：`确认作废该条调休使用记录？作废后使用时长将恢复至调休余额。`；确认后余额恢复，记录保留（status=VOID） |
| I-10 | I-09 后 | 在同一天同一时段重新录入 | **可以成功**（作废时 `active_key` 置 NULL，唯一约束不再拦） |
| I-11 | — | 编辑一条**已作废**的记录 | `已作废的记录不可编辑` |

---

## J. 判重守卫（本轮改造重点）

> 判定为**半开区间**：`aStart < bEnd && bStart < aEnd`。**端点相接不算重叠**。

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| J-01 | 同人同日已有 09:00–12:00（加班转休） | 再录 **12:00–15:00** | ✅ **通过**（端点相接不算重叠） |
| J-02 | 同上 | 再录 **11:00–13:00** | ❌ 拒绝：`该员工在 2026-xx-xx 已存在时间重叠的记录（加班转休 09:00–12:00）。该数据可能已存在，也可能由其他人刚刚提交，请刷新后重试` |
| J-03 | 同理，冲突源为**调休使用** | 再录重叠时段 | 拒绝，冲突项显示 `调休使用 09:00–12:00` |
| J-04 | 同人同日已有一条「其他转休」 | 再录第二条「其他转休」 | ❌ 拒绝：`该员工在 2026-xx-xx 已存在一条「其他转休」记录，同一天不能重复录入。该数据可能已存在，也可能由其他人刚刚提交，请刷新后重试` |
| J-05 | — | 跨模块组合：先录**加班转休** 09:00–12:00，再录**调休使用** 11:00–13:00 | ❌ 拒绝（跨模块判重是明确要求） |
| J-06 | — | 编辑一条记录，把它改成与另一条重叠 | ❌ 拒绝（编辑同样走判重；自身记录会被排除，不会自己撞自己） |
| J-07 | 有一条**已作废**记录 09:00–12:00 | 新录 11:00–13:00 | ✅ 通过（判重只查有效记录，已作废不参与） |
| J-08 | — | **并发**提交两条完全相同的记录 | 只成功一条：DB `active_key` 唯一索引兜底，另一条提示 `该数据可能已存在，也可能由他人刚刚提交，请刷新后重试` |

> `active_key` 格式（勿改）：加班 `userId|yyyy-MM-dd|recordMode|HH:mm|HH:mm`；调休 `userId|yyyy-MM-dd|HH:mm|HH:mm`

---

## K. 写锁（并发保护）

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| K-01 | — | 正常单次提交写请求 | 正常成功（3 秒内抢到锁） |
| K-02 | — | 制造并发长事务占用写锁，再提交另一写请求 | 第二个请求 HTTP **429** + code 429 + `系统繁忙，请稍后重试`（**不排队阻塞**） |
| K-03 | — | 高并发下重复点提交按钮 | 不应出现重复落库（判重 + 写锁共同保证） |
| K-04 | — | 登录接口 `POST /api/auth/login` | **不受**写锁约束（登录排除在外），登录不会被「系统繁忙」挡 |
| K-05 | — | 确认无「写后 3 秒冷却」 | 连续两次写操作间隔很短也应都成功（旧的全站 3s 冷却已移除，是当时的吞吐瓶颈） |

---

## L. 导入 / 导出

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| L-01 | — | 导入 `.xlsx` 正常文件 | 成功行入库；结果 `Result{code:0, success:N, failed:M, errors[]}`；前端 alert 标题 `导入完成：成功 X 条，失败 Y 条` |
| L-02 | — | 导入 `.csv` / `.txt` | `不支持的文件类型，仅支持 .xlsx / .xls 格式` |
| L-03 | — | 不选文件直接导入 | `请选择要导入的文件` |
| L-04 | — | 上传含 **5001 行**数据的文件 | `文件数据行数过多（5001 行），单次最多导入 5000 行，请拆分后重试` |
| L-05 | — | 上传正好 5000 行 | 允许（边界内） |
| L-06 | — | 文件中**部分行**缺员工姓名 / 员工不存在 | **部分成功、不回滚**：成功行照常入库，失败行计入 `errors[]`，逐行提示如 `员工姓名为空`、`未找到员工：xxx` |
| L-07 | — | 数据填在「填写说明」工作表而非「导入模板」 | 提示未读取到任何数据行（请确认数据在第一个工作表「导入模板」中，第一列为员工姓名或工号） |
| L-08 | — | 导出加班/调休数据 | 下载成功；`GET /all-staff-leave/export` 在无数据时提示 `当前查询结果无数据可导出` |

---

## M. 报表与看板

| 编号 | 步骤 | 预期结果 |
|---|---|---|
| M-01 | ADMIN 打开工作台 | 三张卡：员工总数 / 全员可调休余额 / 透支人数，均可点开下钻弹窗 |
| M-02 | EMPLOYEE 打开工作台 | 只见「我的可调休余额」+ 温馨提示 |
| M-03 | 查某部门无透支人员 | 空态文案「暂无透支人员」 |
| M-04 | 统计图：只选起始年月 | `请选择起始年月与结束年月` |
| M-05 | 统计图：起始晚于结束 | `结束年月不能早于起始年月` |
| M-06 | 统计图：跨度 >24 个月 | `时间跨度不能超过 24 个月，请缩小查询范围` |
| M-07 | 员工加班趋势：未选员工 | `请选择员工` |
| M-08 | 员工加班趋势：跨度 >365 天 | `时间跨度不能超过 365 天，请缩小查询范围` |

---

## N. 审计日志

| 编号 | 步骤 | 预期结果 |
|---|---|---|
| N-01 | ADMIN 打开 `/system-log` | 可筛选 模块 / 操作类型 / 级别(信息·警告·错误) / 操作时间 / 关键字；按钮 查询、重置 |
| N-02 | 新增用户 / 编辑用户 / 重置密码 / 冻结 / 启用 / 删除 | 各产生一条对应审计记录 |
| N-03 | 登录失败到锁定、IP 限流 | 产生 WARN 级别审计记录 |
| N-04 | 日历高风险变更 | 记录含 `将 <date> 由「<原类型>」变更为「<新类型>」（高风险：涉及国家法定节假日）` |
| N-05 | **分配角色**（`POST /api/users/{id}/roles`） | ⚠️ **不产生审计记录**——见「已知不一致」 |
| N-06 | 非 ADMIN 访问日志接口 | `无权限：仅管理员可查看系统日志` |

---

## O. 本次修复回归（cefb158 必测）

| 编号 | 步骤 | 预期结果 |
|---|---|---|
| O-01 | `GET /api/auth/health-typo`（白名单前缀 + 不存在的路径） | HTTP **404** + code 404 + `请求的接口不存在`（**修复前是 500**） |
| O-02 | `GET /api/auth/login`（应只允许 POST） | HTTP **405** + `请求方式不支持，该接口仅允许 [POST]`（**修复前是 500**） |
| O-03 | POST 登录接口，body 传 `{bad json` | HTTP **400** + `请求数据格式错误，请检查提交内容`（**修复前是 500**） |
| O-04 | 前端残留调用已删除接口（如 `/api/dict/*`、`/api/upload`） | 统一 404 提示，**不再**出现「服务器内部错误（错误编号 …）」 |
| O-05 | 启动后检查日志 | 未知路径 / 方法不支持 / body 坏 三类均记录为 **WARN**（含简短原因），**不再打完整堆栈、不再记 ERROR** |
| O-06 | 触发一个真正的未预期异常 | HTTP 500 + `服务器内部错误，请稍后重试（错误编号 <12位>）`，**不泄露**堆栈或表结构 |
| O-07 | CORS：`Origin: http://localhost:8601` 请求 | 响应含 `Access-Control-Allow-Origin: http://localhost:8601` 与 `Access-Control-Allow-Credentials: true` |
| O-08 | CORS：`Origin: http://evil.com` | 响应**不带** `Access-Control-Allow-Origin`（浏览器会拦），不返回业务 JSON |
| O-09 | 前端被 401 拦截时的表现 | axios 层清本地 token、跳 `#/login`，提示 `登录已过期，请重新登录` |
| O-10 | 连续两次构建前端 | `static/assets/` 文件数稳定，不出现同页面多份 hash 分片（`emptyOutDir` 已恢复 `true`） |

---

## P. 安全文档暴露面

| 编号 | 前置 | 步骤 | 预期结果 |
|---|---|---|---|
| P-01 | 匿名 | 访问 `/swagger-ui/index.html` 或 `/v3/api-docs` | HTTP 401 + `缺少令牌` |
| P-02 | 非 ADMIN（已登录） | 同上 | HTTP 403 + `无权限访问接口文档` |
| P-03 | ADMIN | 同上 | 可正常打开接口文档 |

---

## 已知不一致 / 待确认（建议评审后决定）

| 编号 | 现象 | 影响 | 建议 |
|---|---|---|---|
| X-01 | 「其他转休」的**备注**：前端标为必填并拦截，**后端无校验**，绕界面调接口可空备注入库 | 同一份数据两条入口两套规则，日后对账/导出可能拿到空备注记录 | 统一为后端必填（与界面一致），或界面改为选填 |
| X-02 | **分配角色不写审计日志**，同模块其它增删改都写了 | 权限变更是最敏感的操作之一，却无审计留痕 | 补一条 `MODULE_USER` / `分配角色` 审计 |
| X-03 | `HolidayService.determineDayType`（:439）为**死代码**（全仓库无调用），它推导的 `ADJUSTED` 会影响「补班日」的测试预期 | 易被误认为补班日返回 `ADJUSTED`；也是本会话第 3 处同类死代码（前两处 `ratioOf` 已删） | 删除该方法，保留 `OvertimeDayType.ADJUSTED` 常量与两处历史数据兼容分支 |
| X-04 | 内置管理员的**改密入口前端未禁用**，靠后端拒绝 | 不算缺陷（后端已挡住），但用户会在界面上白填一次表单才被拒 | 可在界面对 builtin 隐藏改密入口，或加显式说明 |

---

## 汇总统计

| 模块 | 用例数 |
|---|---|
| A 部署与构建验收 | 7 |
| B 登录与会话安全 | 12 |
| C 强制改密与密码策略 | 13 |
| D 登出与令牌黑名单 | 6 |
| E 权限与菜单 | 7 |
| F 内置管理员与用户管理 | 16 |
| G 日历与折算系数 | 16 |
| H 加班录入 | 15 |
| I 调休使用 | 11 |
| J 判重守卫 | 8 |
| K 写锁 | 5 |
| L 导入导出 | 8 |
| M 报表与看板 | 8 |
| N 审计日志 | 6 |
| O 本次修复回归 | 10 |
| P 文档暴露面 | 3 |
| **合计** | **151** |

> 优先级建议：**O（本次修复回归）→ C/D/F（安全与会话）→ J（判重）→ G（日历系数）→ 其余**。
> O 组是本次改动直接引入的行为变化，回归成本最低、价值最高。
