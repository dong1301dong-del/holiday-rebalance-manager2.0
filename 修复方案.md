# 调休管家 修复方案

> 配套文档：`代码审计-冗余与安全风险.md`
> 原则：**分批交付、批内可独立发布、每批可单独回滚**。P0 只做"风险收口"，不改业务语义；行为类修正放 P1；加固与清理放 P2。

---

## 0. 三个需要你先拍板的决策点

这三个点会改变系统行为或运维方式，我不替你决定。

| 编号 | 决策点 | 选项 A | 选项 B | 我的建议 |
|---|---|---|---|---|
| **D1** | 全局写锁 `WriteGate` 的走向 | **保留原设计**（全局串行 + 冷却），仅把"无限等待"改为"有界等待" | 改为**按用户分片锁**，不同用户互不阻塞，彻底消除相互 DoS | 先做 A（P0，零语义变化）；B 作为后续优化 |
| **D2** | "系统配置"页的归宿 | **接上**：让 `ratioOf()`、`dailyCap` 真正读配置 | **下线**：删掉配置页与全套 Config 模块 | 取决于你是否真需要"运维可调系数"。若不需要，B 更诚实 |
| **D3** | 登出语义 | **接受"登出即全设备下线"**（复用 `tokenVersion`，零新增存储） | 引入**令牌黑名单**（精确到单个会话，但需要额外存储与清理） | 内部系统选 A 足够 |

> 下文方案默认按 **D1-A / D2 待定 / D3-A** 展开，D1-B 与 D2-B 作为可选附录。

---

## P0 · 阻断风险（建议当天完成，无业务语义变化）

### P0-1 密钥与数据库口令外置 + 默认值 fail-fast

**问题**：`application.yml` 中 JWT 密钥、`root/root` 直接入库；密钥即签名凭据，泄露即可伪造 admin 令牌。

**改法 1 — 配置改为环境变量注入**（`server-java/src/main/resources/application.yml`）：

```yaml
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/tiaoxiu?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  secret: ${JWT_SECRET:}          # 不再给可用的默认值
  expiration-hours: ${JWT_EXPIRATION_HOURS:12}
```

**改法 2 — 启动自检，默认值直接拒绝启动**（新增 `config/SecurityStartupValidator.java`）：

```java
@Component
public class SecurityStartupValidator implements InitializingBean {

    /** 历史默认密钥，出现即视为未配置 */
    private static final String LEGACY_DEFAULT =
            "tiaoxiu-default-secret-key-please-change-in-prod-2026-08-28-abcdefghijklmnop";

    @Value("${jwt.secret:}") private String jwtSecret;
    @Value("${spring.profiles.active:}") private String profiles;

    @Override
    public void afterPropertiesSet() {
        boolean dev = profiles != null && profiles.contains("dev");
        if (dev) return;   // 开发环境走 application-dev.yml，跳过

        if (jwtSecret == null || jwtSecret.isBlank() || LEGACY_DEFAULT.equals(jwtSecret)) {
            throw new IllegalStateException(
                "启动被拒绝：未配置 JWT_SECRET（生产环境禁止使用默认/空密钥）。"
              + "请通过环境变量注入至少 48 字节的随机串。");
        }
        if (jwtSecret.getBytes(StandardCharsets.UTF_8).length < 48) {
            throw new IllegalStateException("启动被拒绝：JWT_SECRET 长度不足 48 字节（HS256 安全性要求）。");
        }
    }
}
```

**影响面**：生产启动前**必须**设置 `JWT_SECRET` / `DB_PASSWORD` 环境变量，否则**启动失败**（这是刻意设计——宁可起不来，也不要带默认密钥上线）。
**验证**：不设环境变量启动 → 应报错退出；设置后再启动 → 正常。
**回滚**：还原 yml，删除校验类。

---

### P0-2 `WriteGate` 有界等待（消除写锁 DoS）

**问题**：`WriteGate.acquire()` 用 `lock.lock()` **无限等待**，且拦截器在角色校验之前抢锁 → 任意已登录用户持续写请求即可占满 Tomcat 线程池，连读请求一起卡死。

**改法**（`common/WriteGate.java`）——把"无限阻塞"改为"限时获取，失败快速返回"：

```java
/** 获取写锁的最长等待时间；超时即快速失败，避免线程被长期占用拖垮线程池 */
private static final long ACQUIRE_TIMEOUT_MS = 3000;

public void acquire() {
    boolean acquired;
    try {
        acquired = lock.tryLock(ACQUIRE_TIMEOUT_MS, TimeUnit.MILLISECONDS);
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
        throw new BizException(429, "系统繁忙，请稍后重试");
    }
    if (!acquired) {
        // 关键：不再无限排队，直接拒绝，保护线程池
        throw new BizException(429, "系统繁忙（有写操作正在处理），请稍后重试");
    }
    try {
        long wait = cooldownMs - (System.currentTimeMillis() - lastWriteDoneMillis);
        while (wait > 0) {
            Thread.sleep(wait);
            wait = cooldownMs - (System.currentTimeMillis() - lastWriteDoneMillis);
        }
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
    }
}
```

**配套调整**：

1. `WriteGuardInterceptor` 无需改代码——`acquire()` 抛异常时 `preHandle` 不会返回 true，Spring 不会对其调用 `afterCompletion`，**锁不会泄漏**（需在验证清单里专门确认这一点）。
2. 冷却时间从 3000ms 下调为 **800ms**（`app.write-cooldown-ms`），小团队下 3 秒的等待体感很差；若你坚持原值可不动。
3. 在 `GlobalExceptionHandler` 中确保 `code=429` 能被前端识别：前端 `api.js` 只对 HTTP 401 特判，其余走 `code !== 0` 抛错，消息能正常展示，**无需改前端**。

**验证**：用两个并发客户端同时发起写请求 → 第二个应在 3 秒内收到"系统繁忙"提示，而非一直挂起；同时发读请求 → 读请求不受影响、秒回。
**回滚**：还原 `WriteGate.java`。

> ⚠️ 边界：这只解决了"线程池被打穿"。**业务上两个用户仍会串行等待冷却**（这是原设计的意图）。若要彻底消除用户间相互影响，走 D1-B 分片锁。

---

### P0-3 收紧 `ddl-auto`

**问题**：`application.yml` 为 `ddl-auto: update`，生产启动自动改表结构、无回滚。

**改法**：

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate     # 生产：只校验不改结构，不一致则启动失败
```

`application-dev.yml` 保持 `create-drop` 不动（开发用内存库，无风险）。

**影响面**：若线上表结构已有漂移，改为 `validate` 后**启动会失败**——这正是它的价值（把隐性问题显性化）。届时按报错手动补列/改类型，或临时用一次性迁移脚本处理。
**验证**：本地用现有库启动，确认能通过校验。
**回滚**：改回 `update`。

---

## P1 · 行为修正（权限与信息泄露）

### P1-1 `mustChangePwd` 后端强制（堵住默认口令绕过）

**问题**：强制改密只在前端跳转实现，后端零校验，用户可直接调 API 一直用默认口令。

**改法**（`filter/JwtFilter.java`，在 tokenVersion 校验通过之后、`SecurityUtil.set(...)` 之前插入）：

```java
/** mustChangePwd=true 时仍允许访问的接口（否则用户无法改密，会锁死自己） */
private static final List<String> PWD_CHANGE_ALLOWED = List.of(
        "/api/auth/me", "/api/auth/change-password", "/api/auth/logout");

// …… 放在 tokenVersion 校验通过之后：

// 强制改密：默认口令未修改前，除改密相关接口外一律拒绝
if (Boolean.TRUE.equals(user.getMustChangePwd())
        && PWD_CHANGE_ALLOWED.stream().noneMatch(path::startsWith)) {
    writeJson(response, HttpStatus.FORBIDDEN.value(), 403, "首次登录请先修改密码");
    return;
}
```

同时把现有的 `writeUnauthorized` 抽成通用的 `writeJson(response, status, code, msg)`（现在它写死了 401）。

**影响面**：行为收紧，正常用户无感（登录后本来就会被前端引导去改密）。
**验证**：用 `mustChangePwd=true` 的账号登录，拿 token 直接 `GET /api/overtime` → 应返回"首次登录请先修改密码"；`GET /api/auth/me` 应正常。
**回滚**：删除该分支。

---

### P1-2 `assignRoles` 自增 `tokenVersion`（撤权即时生效）

**问题**：`UserService.assignRoles()` 不 bump `tokenVersion`，而授权用的是 token 内嵌 `roles` → 给某人撤权后，其旧 token 最长 24 小时仍按原角色放行。

**改法**（`service/UserService.java` 的 `assignRoles`，先取旧角色做对比，仅在**角色确实变化**时失效旧会话）：

```java
@Transactional
public void assignRoles(Long userId, List<String> roleCodes) {
    if (!userRepository.existsById(userId)) throw new BizException("成员不存在");

    List<String> before = getRoleCodes(userId);           // 新增：记录旧角色
    // ……（原有唯一管理员校验逻辑保持不变）

    userRoleRepository.deleteByUserId(userId);
    if (roleCodes != null) { /* ……原有写入逻辑不变…… */ }

    // 新增：角色集合发生变化时使旧会话失效，保证撤权/提权即时生效
    List<String> after = getRoleCodes(userId);
    if (!new java.util.HashSet<>(before).equals(new java.util.HashSet<>(after))) {
        User u = get(userId);
        u.setTokenVersion(u.getTokenVersion() + 1);
        userRepository.save(u);
    }
}
```

**为什么加"仅变化时"判断**：避免管理员给自己反复提交同一角色时把自己踢下线（唯一 admin 被踢下线会很尴尬）。
**验证**：给用户 A 分配 CLERK → A 的旧 token 立即失效（下次请求返回"登录状态已失效"）；对 A 重复提交相同角色 → 不被踢下线。
**回滚**：删除新增两段。

---

### P1-3 异常信息脱敏

**问题**：`GlobalExceptionHandler.handleOther()` 把 `e.getMessage()` 直接回传，可能泄露 SQL / 类名 / 路径。

**改法**（`common/GlobalExceptionHandler.java`）：

```java
private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(GlobalExceptionHandler.class);

@ExceptionHandler(Exception.class)
@ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
public Result<Void> handleOther(Exception e) {
    String traceId = java.util.UUID.randomUUID().toString().substring(0, 8);
    log.error("未处理异常 traceId={}", traceId, e);      // 详情只进服务端日志
    return Result.fail(500, "服务器内部错误，请稍后重试（追踪号 " + traceId + "）");
}
```

**验证**：人为触发一个 500（如临时改错 SQL），响应中不再包含原始堆栈/类名信息，日志里能用 traceId 定位。
**回滚**：还原方法体。

---

### P1-4 CORS 默认收紧

**问题**：`CorsConfig` 的默认值是 `*` 且 `allowCredentials(true)`，漏配即对任意站点开放带凭证跨域。

**改法**（`config/CorsConfig.java`）：

```java
// 默认值改为空：未显式配置时不放行任何跨域，而不是放行全部
public CorsConfig(@Value("${app.cors-allowed-origins:}") String allowedOrigins,
                  WriteGuardInterceptor writeGuardInterceptor) { ... }

@Override
public void addCorsMappings(CorsRegistry registry) {
    String trimmed = allowedOrigins == null ? "" : allowedOrigins.trim();
    if (trimmed.isEmpty()) {
        log.warn("未配置 app.cors-allowed-origins，已关闭跨域访问（仅同源可访问）");
        return;
    }
    registry.addMapping("/**")
            .allowedOriginPatterns(trimmed.split(","))
            .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true)
            .maxAge(3600);
}
```

`application-dev.yml` / `application.yml` 中已显式配置 `localhost:8601`，保持不动即可。
**验证**：清空配置启动 → 跨域请求被拒、日志有 warn；恢复配置 → 正常。
**回滚**：还原默认值。

---

### P1-5 上传文件校验

**问题**：导入接口只受 20MB 限制，直接交给 EasyExcel 解析，存在 zip bomb / 内存耗尽风险。

**改法**：抽一个共用校验方法（建议放在 `common` 下，供三个导入入口复用），在**解析之前**调用：

```java
public static void validateExcel(MultipartFile file) {
    if (file == null || file.isEmpty()) throw new BizException("文件为空");
    if (file.getSize() > 10L * 1024 * 1024) throw new BizException("文件不得超过 10MB");
    String name = file.getOriginalFilename();
    String lower = name == null ? "" : name.toLowerCase();
    if (!lower.endsWith(".xlsx") && !lower.endsWith(".xls")) throw new BizException("仅支持 .xlsx / .xls 文件");
}
```

并在导入循环中加行数上限（如 `if (rows.size() > 5000) throw new BizException("单次导入不得超过 5000 行");`）。

落点：`OvertimeService.importExcel()`、`LeaveExcelService.importLeaveUsage()`（`ExcelService` 若在 P2 删除则无需处理）。
**验证**：上传 `.txt` 改名文件 → 被拒；上传超限文件 → 被拒；正常模板 → 正常导入。
**回滚**：删除校验调用。

---

## P2 · 加固与清理

### P2-1 登录限流增加 IP 维度

**问题**：锁定只按账号，攻击者可用错误密码把任意账号（含唯一 admin）持续锁死。

**改法**：新增一个内存滑动窗口计数器，按 IP 限制失败次数（如 15 分钟内 30 次），与现有账号锁定**并存**（账号维度保留，新增 IP 维度）。

```java
@Component
public class LoginIpThrottle {
    private static final int MAX_FAILS = 30;
    private static final long WINDOW_MS = 15 * 60 * 1000L;
    private final Map<String, Deque<Long>> fails = new ConcurrentHashMap<>();

    public void check(String ip) {
        if (ip == null) return;
        Deque<Long> q = fails.computeIfAbsent(ip, k -> new ArrayDeque<>());
        synchronized (q) {
            long now = System.currentTimeMillis();
            while (!q.isEmpty() && now - q.peekFirst() > WINDOW_MS) q.pollFirst();
            if (q.size() >= MAX_FAILS) throw new BizException(429, "操作过于频繁，请稍后再试");
        }
    }
    public void recordFailure(String ip) { /* 同理入队 */ }
}
```

在 `AuthService.login()` 开头调 `check(SecurityUtil.getClientIp())`，密码错误分支调 `recordFailure(...)`。

**已知局限（必须写进注释）**：内存实现，**重启即清零、多实例不共享**；若将来多节点部署，需换成 Redis。
**验证**：同一 IP 连续错误 30 次 → 第 31 次返回 429。

---

### P2-2 登出服务端失效（按 D3-A）

**改法**：新增 `POST /api/auth/logout`，`AuthService.logout()` 中 `tokenVersion++` 并保存。

```java
@PostMapping("/logout")
public Result<Void> logout() { authService.logout(); return Result.ok(); }
```

前端 `store.js` 的 `logout()` 改为**先尽力调用**该接口、再清本地状态（网络失败也不能卡住退出）：

```js
export async function logout() {
  try { await authApi.logout(); } catch { /* 忽略：本地状态仍要清 */ }
  auth.token = ''; auth.user = null; auth.menus = []; auth.permissions = [];
  setToken(''); localStorage.removeItem(LAST_ACTIVITY_KEY);
}
```

**代价**：会让该用户**所有设备**的会话失效（管理员也一样）。内部系统可接受。
**验证**：登录 → 复制 token → 前端登出 → 用旧 token 请求 → 应返回"登录状态已失效"。

---

### P2-3 生产关闭 Swagger / H2 控制台

`application.yml` 追加（`application-dev.yml` 反向开启，保持开发期可用）：

```yaml
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
```

### P2-4 死代码清理

按"先删叶子、再删根"的顺序执行，每步后编译验证：

| 步骤 | 动作 | 验证 |
|---|---|---|
| 1 | 删 `web/src/views/Calendar.vue`、`web/src/views/Dict.vue`；清理 `api.js` 中提及它们的注释 | `cd web && npm run build` 成功 |
| 2 | 删后端 `UploadController` + `ExcelService`（连带 `LeaveRequest`、`LeaveRepository` 的引用） | `mvn -DskipTests package` |
| 3 | 删 `LeaveRequest`、`LeaveRepository`，清理 `ExcelService.exportLeave` 相关残留 | 同上 |
| 4 | 删 Dict 全家桶 7 个文件 | 同上 |
| 5 | 删 B 组冗余接口（`/api/holidays` 裸增删、`/api/overtime/resolve`、单条 `/api/overtime`、单条 `/api/leave`、`DELETE /api/leave/{id}`、`/api/leave/messages/{id}/read`、`/api/users/{id}`、`/api/config/{key}`）及对应 `api.js` 方法。**注意 `OvertimeService.resolveDay()` 方法保留**（`createOne` 内部在用），只删 Controller 端点 | 同上 |
| 6 | 删 `reportApi.exportUrl`、`store.js` 的 `isAdmin/isClerk` 导出、`SecurityUtil.getUsername/getRoles/getTokenVersion` | 同上 |

> ⚠️ 第 4/5 步之前先确认：**是否有外部系统/脚本/历史客户端**在调用这些接口。若有，改为先标记 `@Deprecated` + 打日志观察一个周期，再删。

### P2-5 系统配置页落地（按 D2 决策）

- **若 D2-A（接上）**：把 `OvertimeService.ratioOf()` 改为读 `ConfigService`（key：`leave.ratio.workday` / `leave.ratio.rest`），并在 `createOne` 中执行 `overtime.dailyCap` 封顶校验。
- **若 D2-B（下线）**：删 `Config.vue`、`/system/config` 路由、`ConfigController`、`ConfigService`、`ConfigDto`、`SystemConfig`、`SystemConfigRepository`，以及 `DataInitializer.seedConfig()` 与 `DataInitializer` 中对 `configRepository` 的依赖。

---

## 验证清单（每批发布前执行）

1. **编译**：`cd server-java && mvn -DskipTests package`；`cd web && npm run build`。
2. **启动**：设置 `JWT_SECRET` / `DB_PASSWORD` 后启动，确认无 `SecurityStartupValidator` 报错、JPA `validate` 通过。
3. **写锁不泄漏**：并发发 2 个写请求，第二个超时返回 429 后，**再发第 3 个写请求应能正常成功**（证明锁已释放）。
4. **强制改密**：`mustChangePwd=true` 账号 → 业务接口 403、`/api/auth/me` 200。
5. **撤权即时**：改某用户角色 → 其旧 token 立即失效。
6. **主流程冒烟**：登录 → 加班录入（自动折算）→ 调休使用（余额扣减）→ 导出 Excel → 系统日志有留痕 → 登出后旧 token 失效。
7. **回归死代码**：删除后逐页点一遍侧边栏 9 个菜单 + 3 个隐藏页（角色/资源/配置）。

## 回滚策略

- 每批一个独立 commit；P0/P1/P2 之间不合并。
- P0-1 是唯一"会导致启动失败"的改动，回滚只需还原 `application.yml` 并删除校验类。
- 死代码清理如发现遗漏，`git revert` 对应 commit 即可（纯删除，无数据变更）。
- **数据库层面**：本次方案**不删除任何表**。若后续要清 `dict_type`/`dict_data`/`leave_requests` 空表，请先 `mysqldump` 备份再 `DROP`。
