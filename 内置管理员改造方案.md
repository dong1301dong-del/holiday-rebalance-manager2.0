# 内置管理员账号改造方案

> 目标：把 `admin` 改造成**永久内置、权限恒定、密码恒定、前端不可见、自身不参与任何计算**的系统账号。
> 配套：`代码审计-冗余与安全风险.md`、`修复方案.md`

---

## 一、结论先行：6 条需求里，2 条已经做好了

| # | 需求 | 现状 | 需要做什么 |
|---|---|---|---|
| 1 | 永久内置、默认全部权限 | ⚠️ **仅"新装时"成立** | 加内置标记 + 权限改为结构性保证 |
| 2 | 默认密码、不允许变更密码 | ❌ **现状完全相反** | 去掉强制改密 + 拦截改密 + 启动重置 |
| 3 | 不允许变更角色 | ⚠️ 部分（只能靠"唯一 admin"约束） | 加内置标记级硬拦截 |
| 4 | 前端界面不显示 | ⚠️ 部分（下拉/报表已排除，用户管理仍显示） | 补用户列表过滤 |
| 5 | 只允许给他人记录，不能给自己录 | ✅ **已实现** | 巩固即可 |
| 6 | 自身不参与任何计算 | ✅ **已实现** | 巩固即可 |

### 第 5、6 条为什么已经满足

系统里已经存在一条明确的规则，写在 `OvertimeService` 和 `LeaveService` 里：

```java
// OvertimeService.java:128 / LeaveService.java:91
private void requireNotSystemWorker(Long userId) {
    // ADMIN 与 CLERK 属于"系统工作者"，不允许被录入加班/调休数据
    ... if (r != null && ("ADMIN".equals(r.getCode()) || "CLERK".equals(r.getCode())))
        throw new BizException("系统管理员与录入员不允许被录入加班/调休数据");
}
```

调用点覆盖了全部写入路径：

- 加班：`createOne`(456)、`update`(540)、`importExcel`(762)
- 调休：`requireUser`(644，创建入口)、`update`(381)

报表侧也已排除：

```java
// ReportService.java:48
private static final Set<String> EXCLUDE_ROLE_CODES = Set.of("ADMIN", "CLERK");
```

用于 `dashboard()`（员工总数 / 总余额 / 透支人数）、`allStaffLeave()`、月度加班/调休统计。

**所以第 5、6 条不用新建机制，只需要把"按角色判断"升级为"按内置标记判断 + 角色判断"双保险**（原因见 4.6）。

---

## 二、⚠️ 必须先解决的"自锁死循环"

这是本次改造里**最容易踩且最致命**的一点，必须先说：

`DataInitializer.seedAdmin()` 目前设的是：

```java
admin.setMustChangePwd(true);   // 首次登录强制改密
```

而你的需求是"**不允许变更密码**"。这两条直接冲突，会导致：

> 管理员登录 → 前端强制跳转改密页 → 改密被后端拒绝 → **管理员永远进不了系统**。

**所以以下三件事必须同时改，缺一个就出死锁**：

1. `seedAdmin()` 里 `mustChangePwd` 由 `true` 改 **`false`**；
2. 拦截 `change-password` 对内置管理员的调用；
3. 前端对内置管理员隐藏"修改密码"入口。

---

## 三、现状盘点（改造涉及的落点）

| 关注点 | 位置 | 现状 |
|---|---|---|
| 管理员创建 | `DataInitializer.seedAdmin()` (290) | 仅当用户表为空时执行；`mustChangePwd=true`；密码硬编码 `Abc_123456` |
| 权限来源 | `ResourceService.buildMenu()` (52) | **按"角色-资源绑定"聚合**，不特判 ADMIN |
| 绑定播种 | `DataInitializer.seedRoleResources()` (253) | `ADMIN = 全部资源`，但**只在绑定表为空时执行一次** |
| 改密 | `AuthService.changePassword()` (136) | 无任何内置账号拦截 |
| 重置密码 | `UserService.resetPassword()` | 无内置账号拦截 |
| 冻结/解冻 | `UserService.setStatus()` | 无内置账号拦截 |
| 删除 | `UserService.delete()` | 无内置账号拦截，**也没有"不能删自己"保护** |
| 角色分配 | `UserService.assignRoles()` (225) | 有"唯一 admin"约束，但**未禁止改内置账号** |
| 用户列表 | `UserService.list()` (60) | `findByStatusNot(DELETED)` → **管理员赫然在列** |
| 前端员工下拉 | `Overtime.vue:57`、`Leave.vue:107` | 已按角色过滤掉 ADMIN/CLERK ✅ |
| 顶栏余额/个人中心 | `MainLayout.vue`、`Profile.vue` | 按登录用户直接取余额，无内置判断 |

**两个结构性缺口**：

1. **权限不是"永久全部"**：`buildMenu` 只认绑定关系。如果日后在「资源管理」里新增一个菜单、或误删了 ADMIN 的某条绑定，**管理员就会真的丢掉权限**，而且没有任何提示。
2. **没有自我保护**：删掉/冻结自己的路径是通的，唯一管理员一旦自锁，系统再也进不去。

---

## 四、改造方案

### 4.1 数据模型：新增内置标记 + 老库迁移

`entity/User.java`：

```java
/** 内置管理员账号的用户名（全局唯一） */
public static final String BUILTIN_ADMIN_USERNAME = "admin";

@Column(nullable = false)
private Boolean builtin = false;
```

`DataInitializer` 新增幂等迁移（放在 `seedAdmin()` 之后），解决"老库里已有 admin 但没有标记"的问题：

```java
private void ensureBuiltinAdmin() {
    User admin = userRepository.findByUsername(User.BUILTIN_ADMIN_USERNAME).orElse(null);
    if (admin == null) return;                       // 全新库由 seedAdmin 负责创建

    admin.setBuiltin(true);
    admin.setStatus(User.STATUS_ACTIVE);             // 内置账号永远可用
    admin.setMustChangePwd(false);                   // 取消强制改密（否则死锁）
    admin.setLoginFailCount(0);                      // 解除历史锁定
    admin.setLoginLockedUntil(null);
    admin.setPassword(PasswordUtil.encode(builtinAdminPassword));  // 每次启动重置密码
    userRepository.save(admin);

    // 角色强制归位：只保留 ADMIN，清掉一切多余绑定
    userRoleRepository.deleteByUserId(admin.getId());
    Role adminRole = roleRepository.findByCode(Role.CODE_ADMIN).orElseThrow();
    UserRole ur = new UserRole();
    ur.setUserId(admin.getId());
    ur.setRoleId(adminRole.getId());
    userRoleRepository.save(ur);
}
```

> **注意**：密码在**每次启动**都会重置。这同时顺手解决了"管理员被 5 次错误密码锁死 15 分钟、没人能救"的窘境。

### 4.2 永久全部权限（改成结构性保证）

`ResourceService.buildMenu()` 增加 ADMIN 特判——**不再依赖绑定关系**：

```java
public MenuResult buildMenu(List<String> roleCodes) {
    List<Resource> resources;
    if (roleCodes != null && roleCodes.contains(Role.CODE_ADMIN)) {
        // 管理员：结构性拥有全部启用资源。新增菜单自动拥有，误删绑定也不受影响
        resources = resourceRepository.findAll().stream()
                .filter(r -> Resource.STATUS_ACTIVE.equals(r.getStatus()))
                .collect(Collectors.toList());
    } else {
        // ……原有：按角色-资源绑定聚合……
    }
    List<String> permissions = resources.stream().map(Resource::getCode).collect(Collectors.toList());
    return new MenuResult(buildTree(resources), permissions);
}
```

**为什么安全**：`buildTree()` 只把 `type == MENU` 的资源渲染成菜单（`ResourceService.java:97`），按钮类资源只会进 `permissions`，不会变成菜单项。而且改造前 ADMIN 本就绑定了全部资源，**菜单表现完全不变**，只是从"靠数据"变成"靠代码"。

### 4.3 密码固定不可改

| 位置 | 改动 |
|---|---|
| `DataInitializer.seedAdmin()` | `mustChangePwd` 改 `false` |
| `DataInitializer.ensureBuiltinAdmin()` | 启动时强制重置为默认密码 |
| `AuthService.changePassword()` | 开头加：`if (Boolean.TRUE.equals(user.getBuiltin())) throw new BizException("内置管理员账号密码不可修改");` |
| `UserService.resetPassword()` | 加 `requireNotBuiltin(userId)` |
| `Profile.vue` | 内置管理员隐藏"修改密码"卡片 |

**密码来源建议**：不要硬编码在 `application.yml`，改为配置项 + 环境变量回落：

```yaml
app:
  builtin-admin-password: ${BUILTIN_ADMIN_PASSWORD:Abc_123456}
```

效果完全一样（密码不可改、永远可用、重启恒定），但密码本身不写进仓库。**如果你坚持要写死在代码/配置里也能做，只是等于公开**。

### 4.4 角色/状态不可变 + 自锁防护

在 `UserService` 加两个守卫，并挂到所有「变更他人」的方法上：

```java
/** 内置管理员账号不允许被任何操作改动 */
private void requireNotBuiltin(Long userId) {
    if (Boolean.TRUE.equals(get(userId).getBuiltin()))
        throw new BizException("内置管理员账号不允许被修改、冻结或注销");
}

/** 任何人都不能对自己执行该操作（防自锁） */
private void requireNotSelf(Long userId) {
    if (userId.equals(SecurityUtil.getUserId()))
        throw new BizException("不能对自己执行该操作");
}
```

挂载点：

| 方法 | `requireNotBuiltin` | `requireNotSelf` |
|---|---|---|
| `delete(userId)` | ✅ | ✅ |
| `setStatus(userId, status)` | ✅ | ✅ |
| `assignRoles(userId, codes)` | ✅ | —（本来就不允许改自己角色） |
| `resetPassword(userId, pwd)` | ✅ | ✅ |
| `update(userId, req)` | ✅ | 允许（本人可改姓名/部门） |

> 现有的"系统只能有一个 admin""不能取消唯一管理员的权限"两条约束**保留**，只是把判定基准从"是否持有 ADMIN 角色"改为"是否 `builtin`"，更稳。

### 4.5 前端界面不显示

| 位置 | 改动 |
|---|---|
| `UserService.list()` | 过滤掉 `builtin`：`.filter(u -> !Boolean.TRUE.equals(u.getBuiltin()))` |
| `UserService.get(id)` | **不过滤**（内部逻辑与本人查详情仍需能取到） |
| `Overtime.vue` / `Leave.vue` 员工下拉 | 在现有角色过滤上，再加 `!u.builtin` |
| `ReportService` | `EXCLUDE_ROLE_CODES` 之外再叠加 `builtin` 兜底 |
| `AllStaffLeave.vue` / `ChartStats.vue` 部门选项 | 因来源是 `userApi.list()`，随列表过滤自动生效 |
| `AuthDto.UserInfo` | 增加 `builtin` 字段下发，供前端判断 |
| `MainLayout.vue` 顶栏 | 内置管理员隐藏"我的余额"数字，保留"透支预警"（管理员需要看） |
| `Profile.vue` | 隐藏"我的余额""我的记录"，隐藏"修改密码"卡片 |

> **一处副作用需知**：`/api/users` 同时服务于「用户管理列表」和「员工下拉」。过滤后，如果**只有 admin 在"管理部"**，则该部门不会再出现在部门筛选里——这符合"不显示"的预期。

#### 4.5.1 「不显示」的准确含义（数据库里到底还有没有）

这里要区分三层，**千万不能混为一谈**：

| 层 | 状态 | 说明 |
|---|---|---|
| **数据库** | ✅ **完整保留**，一行不少 | `users` 表里那一行不动；`user_roles` 到 ADMIN 角色的绑定不动；`roles` 表里的 `ADMIN` 角色也不动 |
| **接口** | ⚠️ **仅 `GET /api/users` 过滤** | 返回前跳过 `builtin` 账号。**其他接口一律不受影响** |
| **前端** | ✅ 看不到 | 用户管理列表、员工下拉、部门筛选里都不会出现 |

**注册登录完全不受影响**：登录走 `POST /api/auth/login`，内部是 `userRepository.findByUsername(...)` **直接按账号名查库**，不经过用户列表接口。`GET /api/auth/me` 同理走 `findById`。所以管理员照样能登录、能拿全部权限。

**内部约束也不受影响**（已核实）：`countAdmins()`（唯一管理员判定）与 `getRoleCodes()` 都是**直接查仓储**，不调用 `UserService.list()`；而 `list()` 全项目**只有 `UserController.list()` 一个调用方**。所以在 `listResponses()` 这一层过滤是安全的，不会破坏"系统只能有一个管理员"的约束。

**⚠️ 一个会被误判的坑**：因为界面上看不见，如果有人以为账号被删了、想重新建一个 `admin`，会收到"**用户名已存在**"的报错——看不见却占着名字，很容易让人困惑。

针对这一点，有三种做法可选：

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A（我原方案）** | 彻底从列表隐藏 | 最彻底、最干净 | "看不见却占着用户名"，易被误判为已删除 |
| **B** | 仍然显示，但整行**只读**、操作按钮全部禁用，并标注"内置"标签 | 透明，不会有人误以为被删或想重建 | 与"前端不显示"的要求不完全一致 |
| **C（推荐折中）** | 列表**隐藏该行**，但在「用户管理」页顶部加一行**只读提示**："系统内置管理员账号不在本列表中" | 既满足"不显示"，又消除困惑 | 需要在页面上加一行静态文案 |

### 4.6 不参与计算（巩固已有规则）

`OvertimeService.requireNotSystemWorker` 与 `LeaveService.requireNotSystemWorker` 各加一行：

```java
if (Boolean.TRUE.equals(u.getBuiltin()))
    throw new BizException("内置管理员账号不参与加班/调休计算");
```

**为什么要加**：现在的判断依赖"持有 ADMIN/CLERK 角色"。虽然 4.4 已经禁止改内置账号的角色，但多一道与角色解耦的硬判断，可以保证**即使角色表被异常改动，管理员也不会被算进业务数据**。

**保持 CLERK 仍被排除**（不改成只判 builtin），避免行为回归。

---

## 五、需要你拍板的 3 个点

| 编号 | 决策点 | 选项 | 我的建议 |
|---|---|---|---|
| **B1** | 默认密码写在哪 | A：硬编码在 `application.yml`<br>B：配置项 + 环境变量回落 | **B**。同样"永久可用、不可改"，但密码不进仓库 |
| **B2** | 内置管理员是否豁免登录锁定 | A：豁免（避免被人用错误密码把唯一管理员锁死）<br>B：保留锁定（更符合安全惯例） | **A**。因为密码本就固定不可改，锁定挡不住有密码的人，只会挡你自己 |
| **B3** | 系统日志里是否隐藏 admin 的操作 | A：保留（可追溯谁用管理员账号做了什么）<br>B：隐藏 | **A**。这是审计资产，隐藏反而降低可追溯性 |

---

## 六、必须一起改的先后顺序

```
① seedAdmin: mustChangePwd true → false      ─┐
② ensureBuiltinAdmin()：标记 + 重置密码 + 角色归位 ─┼→ 否则管理员登录即死锁
③ AuthService.changePassword 拦截内置账号      ─┘

④ User.builtin 字段 + 老库迁移
⑤ buildMenu 的 ADMIN 特判（全部权限）
⑥ UserService 四个守卫（不可改/不可删/不可冻结/不可自锁）
⑦ UserService.list 过滤 + AuthDto 下发 builtin
⑧ requiredNotSystemWorker 追加 builtin 判断
⑨ 前端：Profile / MainLayout 隐藏相关入口
```

①②③ 是**原子改动，必须同一次发布**。

---

## 七、验证清单

1. **启动**：老库启动后，`admin` 的 `builtin=true`、密码被重置为配置值、角色仅有 ADMIN。
2. **登录**：`admin` 登录后**不再被强制跳转改密页**，能正常进入工作台。
3. **改密被拒**：在个人中心点改密 → 应提示"内置管理员账号密码不可修改"；直接调 `POST /api/auth/change-password` → 同样被拒。
4. **权限恒定**：在「资源管理」新增一个菜单 → 管理员刷新后**立即拥有**；在「角色权限」里把 ADMIN 的资源全部取消 → 管理员刷新后**权限不变**。
5. **不可改角色**：对 `admin` 调 `assignRoles` / `resetPassword` / `setStatus` / `delete` → 全部返回"内置管理员账号不允许被…"。
6. **防自锁**：登录任意账号后删除自己 → 返回"不能对自己执行该操作"。
7. **不可录入**：在加班/调休页面用员工下拉，**找不到 admin**；直接构造 `POST /api/overtime` 带 admin 的 userId → 返回"内置管理员账号不参与加班/调休计算"。
8. **不显示**：用户管理列表无 admin；部门筛选无"管理部"（若无其他人在该部门）；工作台"员工总数"不含 admin。
9. **重启恒定**：手工把 admin 密码/角色改乱 → 重启服务 → 自动恢复。

---

## 八、必须提醒你的一个代价

这套改造的实质是：**用一个"永远存在、永远可用、口令固定"的管理员账号，换取"永远不会把自己锁在门外"的运维确定性。**

这正是审计报告里标记为风险的形态（详见 `代码审计-冗余与安全风险.md` 的 H1 与 M7）。既然这是你明确要的取舍，那么补偿措施就变得**必须做**，否则这个账号会成为整个系统的固定后门：

1. **JWT 密钥必须外置**（`修复方案.md` P0-1）——否则攻击者连密码都不需要，伪造令牌即可。
2. **登录加 IP 维度限流**（`修复方案.md` P2-1）——固定密码 + 无限尝试 = 必然被爆破。
3. **网络层限制**：把 `admin` 的登录来源限制在内网 / 白名单 IP（网关或反向代理层做，最省事也最有效）。
4. **系统日志必须保留 admin 的操作痕迹**（即 B3 选 A），一旦出事能追溯。

> 一句话：**账号可以固定，但它的"入口"必须收紧。** 只做前者不做后者，等于把钥匙焊在门上。
