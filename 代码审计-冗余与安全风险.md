# 调休管家 代码审计报告：冗余代码与安全风险

> 审计对象：`holiday-rebalance-manager2.0`（server-java + web）
> 审计方式：全量引用关系扫描（Java 类 / 方法 / 接口路径 vs 前端调用），逐条人工确认
> 结论口径：「可删」= 删除后当前前端与后端运行时行为无任何变化

---

## 摘要

| 维度 | 结论 |
|---|---|
| 可整块删除的文件 | 13 个，约 **1441 行**（前端 2 个页面 + 后端 11 个类） |
| 未被前端调用的后端接口 | **12 个** |
| 未被调用的前端 API 封装方法 | **10 个** |
| **最值得注意的问题** | 「系统配置」页是**装饰性**的——6 个配置项存了、能改，但业务代码从不读取，折算系数实际硬编码 ✅ |
| 安全问题 | 高危 **3** 项、中危 **7** 项、低危/加固 **8** 项 |

**最需要立刻处理的两件事**：① 配置文件里硬编码的 JWT 密钥与数据库口令；② 全局写锁 `WriteGate` 带来的拒绝服务面。详见第二部分。

---

## 第一部分 · 冗余 / 死代码清单

### A. 可整块删除（无任何引用链）

这些文件之间互相引用，构成封闭的死链，删除后不影响任何现存功能。

| # | 文件 | 行数 | 判定依据 |
|---|---|---|---|
| 1 | `web/src/views/Calendar.vue` | 260 | 路由表 `router.js` 未注册 `/calendar`；无任何 `import`；自身注释已注明「历史遗留」；且 import 了 api.js 中**不存在**的 `statsApi`（一旦被引用会直接构建失败） |
| 2 | `web/src/views/Dict.vue` | 190 | 同上；import 了不存在的 `dictApi` |
| 3 | `server-java/.../controller/UploadController.java` | 139 | 4 个接口（`/api/import/overtime`、`/api/template/overtime`、`/api/export/overtime`、`/api/export/leave`）前端 `api.js` 中**一个都没有**；类注释自认「早期版本实现，保留仅为兼容历史前端调用」 |
| 4 | `server-java/.../service/ExcelService.java` | 237 | 唯一调用方就是 `UploadController`（加班模块的新导出走 `OvertimeService` 自己的 `importExcel/exportExcel/template`） |
| 5 | `server-java/.../controller/DictController.java` | 106 | 前缀 `/api/dict`，前端无 `dictApi`，零调用 |
| 6 | `server-java/.../service/DictService.java` | 98 | 仅被 DictController 调用 |
| 7 | `server-java/.../dto/DictDto.java` | 33 | 仅被 DictController / DictService 调用 |
| 8 | `server-java/.../entity/DictType.java` | 61 | 仅被上两者引用 |
| 9 | `server-java/.../entity/DictData.java` | 68 | 同上 |
| 10 | `server-java/.../repository/DictTypeRepository.java` | 41 | 仅被 DictService 引用 |
| 11 | `server-java/.../repository/DictDataRepository.java` | 34 | 同上 |
| 12 | `server-java/.../entity/LeaveRequest.java` | 114 | 「请假审批流」实体，但系统**没有审批流**（`LeaveController` 已改为无审批、直接扣减）；仅被 UploadController 与 ExcelService.exportLeave 使用 |
| 13 | `server-java/.../repository/LeaveRepository.java` | 60 | 自身注释白纸黑字写着：「**LeaveRequest 及其表已废弃，仅为兼容历史数据保留**」 |

删除 A 组的连带处理：

- `main.js` 无需改动（`Calendar.vue`/`Dict.vue` 本就未注册）。
- `api.js` 第 229、275 行的注释提到 `Calendar.vue`，属注释，可顺手清理。
- `DataInitializer.java` 中判断 legacy 资源的 `resourceRepository.findByCode("dict:view")` 逻辑，属于「资源树重建触发条件」，删掉 Dict 后建议保留（它本就是用来清理历史遗留的）。
- 数据库中的 `dict_type` / `dict_data` / `leave_requests` 三张表：实体删除后仍是**空表且不再被 JPA 维护**。如需彻底清理，请**先备份再手动 DROP**（JPA 不会自动删表）。

### B. 接口级冗余（后端保留接口，但前端从不调用）

删除这些 `@XxxMapping` 方法不影响前端；对应 `api.js` 中的封装方法可一并删除。

| 后端接口 | 前端对应封装 | 说明 |
|---|---|---|
| `GET /api/holidays` | `holidayApi.list()` | 裸列表接口，前端只用 `/year`、`/month`、`/resolve` |
| `POST /api/holidays` | `holidayApi.upsert()` | 前端保存走 `POST /api/holidays/month` |
| `DELETE /api/holidays/{date}` | `holidayApi.remove()` | 前端无删除入口 |
| `GET /api/overtime/resolve` | `overtimeApi.resolveDay()` | **功能重复**：前端实际调用的是 `holidayApi.resolve()` → `/api/holidays/resolve` |
| `POST /api/overtime`（单条） | `overtimeApi.create()` | 页面统一走 `/batch` |
| `POST /api/leave`（单条） | `leaveApi.create()` | 页面统一走 `/batch` |
| `DELETE /api/leave/{id}` | `leaveApi.remove()` | 页面用「作废」`/void`，不用物理删除 |
| `POST /api/leave/messages/{id}/read` | `leaveApi.readMessage()` | 前端有消息角标，但**没有任何地方调用已读接口**，消息不会被标记已读 |
| `GET /api/users/{id}` | `userApi.get()` | 前端只用列表 |
| `GET /api/config/{key}` | `configApi.get()` | 前端只用 `list` / `set` |
| `GET /api/auth/health`、`GET /api/auth/health-root` | — | 健康探针，**建议保留**（监控可能用到） |

> 注意：`OvertimeService.resolveDay()` 这个**方法本身不能删**——`createOne()` 内部在用它算系数，只有 `OvertimeController` 上那个 `/resolve` 端点可以删。

### C. 方法 / 导出级冗余

| 位置 | 项 | 说明 |
|---|---|---|
| `web/src/api.js` | `reportApi.exportUrl = '/api/report/export'` | **指向一个不存在的后端接口**（`ReportController` 只有 `/all-staff-leave/export`） |
| `web/src/store.js` | `isAdmin()`、`isClerk()`、`hasRole()`（对外导出） | 全项目零外部调用（`isEmployee` / `canManage` 在用，`hasRole` 仅被 `isEmployee` 内部使用） |
| `common/SecurityUtil.java` | `getUsername()`、`getRoles()`、`getTokenVersion()` | 全项目零调用 |
| `repository/LeaveRepository.java` | 4 个查询方法（`findByUserIdOrderByCreatedAtDesc` 等） | 零调用（唯一使用者 UploadController 只用 `findAll()`）——随 A 组一起删 |
| `service/ExcelService.java` | `importOvertime/overtimeTemplate/exportOvertime/exportLeave` | 随 A 组一起删 |

### D. ⚠️ 重点：系统配置页是「装饰性」的

这是本次审计**比死代码更值得注意**的发现：

- `DataInitializer` 启动时播种了 6 个配置项：`system.name`、`overtime.dailyCap`、`overtime.period.am`、`overtime.period.pm`、`leave.ratio.workday`、`leave.ratio.rest`；
- 前端有完整的「系统配置」页面（`Config.vue`）可以编辑它们，改完「即时生效」的注释也写了；
- **但全项目业务代码从不读取这些值**。折算系数实际硬编码在 `OvertimeService.ratioOf()`：

```java
public static BigDecimal ratioOf(String dayType) {
    if (DAY_WORKDAY.equals(dayType) || "ADJUSTED".equals(dayType)) return new BigDecimal("0.5");
    return BigDecimal.ONE;   // 休息日 / 法定节假日
}
```

后果：

1. 管理员在配置页把「工作日折算比例」从 0.5 改成 0.8，**实际不生效**，且没有任何提示——属于「功能误导」，比单纯死代码更危险。
2. `overtime.dailyCap`（单日加班封顶 7.5h）**从未被校验**，即单日加班时长没有封顶保护。
3. `SystemConfig` 表唯一被真正读取的路径是 `ConfigService.get()`，而它只被那个**死接口** `GET /api/config/{key}` 调用。

**两个方向二选一**：
- 若要保留配置页 → 把 `OvertimeService.ratioOf()` 改为读 `ConfigService`，并补上 dailyCap 校验；
- 若确认不需要 → 连同 `Config.vue`、`ConfigController`、`ConfigService`、`SystemConfig`、`SystemConfigRepository` 与 `/system/config` 路由一并下线，避免误导。

---

## 第二部分 · 安全风险分析

### 风险汇总（按等级）

| 等级 | 数量 | 事项 |
|---|---|---|
| 🔴 高危 | 3 | 硬编码密钥 / 全局写锁 DoS / 生产自动改表 |
| 🟠 中危 | 7 | 异常信息泄露 / 强制改密可绕过 / 撤权不生效 / 登出无服务端失效 / CORS 默认通配 / 上传无校验 / 登录锁定可致 DoS |
| 🟡 低危·加固 | 8 | token 存 localStorage / 无 HTTPS / Swagger 暴露 / H2 控制台 / 账号枚举 / 审计日志敏感 / 启动重建资源树 / 前端 HTML 渲染 |

### 🔴 高危

**H1. 配置文件硬编码密钥与数据库口令**

`application.yml`：

```yaml
spring.datasource.username: root
spring.datasource.password: "root"
jwt.secret: "tiaoxiu-default-secret-key-please-change-in-prod-2026-08-28-abcdefghijklmnop"
```

- JWT 采用 HS256 对称签名，**密钥即签名凭据**。密钥随源码提交、且形态明显是「待替换的默认值」，任何能拿到源码/仓库的人都可以离线签发一个 `roles:["ADMIN"]` 的合法 token，直接以管理员身份调用全部接口（后端只验签、不比对任何外部状态）。
- 数据库口令 `root/root` 一并入库。
- README 里虽然写了「生产部署请修改」，但这属于**依赖人工纪律**的控制，而非技术控制。

> 建议：密钥与口令一律走环境变量 / 外部配置中心（`${JWT_SECRET}`），启动时若检测到等于默认值直接**拒绝启动**（fail-fast）；密钥改为随机 64+ 字节；同时换用 RS256 可进一步降低泄露影响面。

**H2. `WriteGate` 全局写锁 = 单点拒绝服务**

设计上所有写请求（POST/PUT/DELETE）全局串行，且两次写之间强制 `Thread.sleep` 等待 3 秒冷却。问题有三层：

1. **拦截器在角色校验之前执行**。`WriteGuardInterceptor.preHandle()` 先抢锁，`Controller` 里的 `requireManage()` 才判权限。因此**任意已登录用户**（哪怕是最低权限员工）只要持续 POST 任意接口（哪怕必然返回「无权限」），就能持续占用全局写锁。
2. **持锁期间线程在 `sleep`**。Tomcat 默认 200 个工作线程，被锁阻塞的请求会一直占着线程不退。攻击者用几十个并发写请求即可把线程池打满，导致**读请求也一起卡死**——影响面从「写」扩大到「全站」。
3. 即使没有恶意，业务上 A、B 两人同时提交，后者至少要等 3 秒，且并发量一上来呈线性排队。

> 建议：① 把并发控制从「全局单锁」改为**按键（如 userId 或记录 ID）分片锁**；② 冷却时间下调或改为「提示重试」而非阻塞线程；③ 权限校验前置到拦截器/过滤器，未授权请求不得进入写闸门；④ 若确需全局限流，用令牌桶 + 快速失败（返回 429），**不要 sleep 占线程**。

**H3. `ddl-auto: update` 用于生产**

`application.yml` 中 JPA 为 `ddl-auto: update`，应用启动即自动变更表结构。生产环境一旦实体与线上结构不一致，可能自动加列、改类型，且**没有回滚**，属于不可控的数据风险。

> 建议：生产改 `none` 或 `validate`，表结构变更走版本化迁移脚本（Flyway/Liquibase）。

### 🟠 中危

**M1. 异常信息直接回传客户端**

`GlobalExceptionHandler.handleOther()`：

```java
return Result.fail(500, "服务器内部错误：" + e.getMessage());
```

把原始异常 message 拼给前端，可能泄露 SQL 片段、表名、类名、文件路径。代码注释里也自认「正式环境若担心信息泄露可改为通用文案」。

> 建议：对外返回通用文案 + 一个追踪 ID，详细信息只落服务端日志。

**M2. `mustChangePwd` 只是前端控制，可绕过**

管理员重置密码 / 新建用户后 `mustChangePwd = true`，后端把它回传，`Login.vue` 据此跳转 `/change-password`。**但后端没有任何接口校验这个标志**，`router.js` 的守卫也只校验 token/user/permission，不看 `mustChangePwd`。

后果：用户拿到默认口令 `Abc_123456` 后，不访问登录页跳转，直接在浏览器地址栏进 `#/dashboard`，或直接携带 token 调用任意 API，即可**始终使用默认口令**。

> 建议：在 `JwtFilter` 或一个专用拦截器中拦截：`mustChangePwd=true` 时只放行 `change-password` 与 `me`，其余一律 403。

**M3. 角色变更不使旧令牌失效**

`UserService.assignRoles()` 只重写 `user_role` 绑定，**没有自增 `tokenVersion`**；而 `JwtFilter` 是从 token 的 `roles` claim 取角色用于授权。因此**给某人降权（如取消 ADMIN）后，其已持有的 token 在最长 24 小时（默认有效期）内仍按旧角色放行**。

> 建议：`assignRoles` 中同步 `tokenVersion++`（或改为每次请求从库里读角色）。

**M4. 登出没有服务端失效**

`logout()` 仅清空 localStorage。JWT 无状态、无黑名单，token 在到期前**始终有效**。若 token 曾泄露，用户无法主动使其失效。

> 建议：引入短时效 access token + 刷新令牌，或维护一个 `tokenVersion` 级别的吊销能力（当前已有版本号机制，可复用）。

**M5. CORS 默认值过于宽松**

`CorsConfig` 中 `app.cors-allowed-origins` 默认值是 `*`，且 `allowCredentials(true)`。虽然 dev/prod 配置里写的是 `localhost:8601`，但**只要某个环境漏配该项**，`allowedOriginPatterns("*")` + 凭证模式会回显任意 Origin，等价于对任意网站开放带凭证的跨域。

> 建议：默认值改为空，未显式配置时**不放行**；生产用精确域名白名单。

**M6. 文件上传缺少类型与内容校验**

加班/调休导入接口只受 `spring.servlet.multipart.max-file-size: 20MB` 约束，随后直接把流交给 EasyExcel 解析。20MB 的 xlsx 可以解压成极大的 XML（zip bomb），或构造畸形结构导致内存/CPU 耗尽。

> 建议：校验扩展名与 `Content-Type`、限制解析行数、使用 POI/EasyExcel 的流式模式并设置解压上限。

**M7. 登录锁定可被滥用为账号拒绝服务**

连续 5 次密码错误即锁定账号 15 分钟，且**只按账号维度、不按来源 IP**。攻击者可用错误密码反复触发，把任意账号（包括唯一的 admin）持续锁死，正常用户无法登录；同时没有验证码/滑块等人工成本。

> 建议：改为「按 IP + 按账号」双维度，并对同一 IP 的失败次数单独限流；或引入渐进式延迟与验证码。

### 🟡 低危 / 加固建议

| # | 事项 | 说明与建议 |
|---|---|---|
| L1 | token 存于 `localStorage` | 一旦出现 XSS 即可被窃取；当前未发现注入点，但建议改 HttpOnly Cookie（需处理 CSRF）或缩短有效期 |
| L2 | 无 HTTPS/TLS 配置 | token 与口令明文传输，建议在网关/反向代理层强制 TLS 并加 HSTS |
| L3 | Swagger/springdoc 在生产暴露 | `JwtFilter` 只按扩展名放行静态资源，`/swagger-ui/*.js/.css` 等可匿名访问；建议生产关闭 `springdoc` 或整体置于鉴权后 |
| L4 | dev 环境 H2 控制台（空口令） | `application-dev.yml` 开启 `/h2-console`；虽被 JwtFilter 拦在门外，但绝不可在生产 profile 启用 |
| L5 | 账号枚举 | 5 次失败后返回「账号已锁定」，可区分账号是否存在；建议锁定提示也保持同一文案 |
| L6 | 审计日志记录 before/after 全量值 | `AuditLog` 的 `beforeValue/afterValue` 可能写入敏感字段，建议脱敏 |
| L7 | 启动时可能重建资源树 | `DataInitializer.migrateRolesAndCleanResources()` 在某些版本特征下会 `deleteAll()` 全部资源与角色绑定，靠代码注释自我约束「仅开发期」；生产启动有清空菜单配置的风险，建议加开关或改为幂等增量 |
| L8 | 前端 `dangerouslyUseHTMLString` | `Holiday.vue:165` 用 `ElMessageBox.alert(data.warnings.join('<br/>'))` 渲染后端文案。当前 warnings 只由「日期 + 固定类型标签」构成，风险低；但若未来把用户可编辑的假期名称拼进去，就会变成存储型 XSS，建议改为纯文本渲染 |

### ✅ 做得好的地方（不要改坏）

- **SQL 注入面为零**：全部使用 Spring Data JPA / JPQL 命名参数（`:userId`），无 `JdbcTemplate`、无字符串拼接查询、无 `nativeQuery`。
- **密码存储规范**：BCrypt（自带随机盐），并有一/二/三级强度校验，准入门槛 8–20 位含字母+数字。
- **接口级鉴权覆盖较全**：写操作基本都有 `requireAdmin()` / `requireManage()`，`UserController` 8 个接口逐条把关。
- **数据权限收敛正确**：员工查询被强制改写为本人 ID（`scopeUserId`），列表、余额、报表均生效，无越权看他人数据的路径。
- **`SecurityUtil` 的 ThreadLocal 在 `finally` 中清理**，避免线程池复用导致身份串号。
- **唯一管理员约束**：系统强制保留且仅允许一个 admin，防止「删光管理员把自己锁死」。
- **登录响应不区分「账号不存在」与「密码错误」**，基础防枚举到位。

---

## 附：本次审计的核验方法

1. **类级引用扫描**：遍历 `com/tiaoxiu` 下全部 `.java`，统计每个类名在其它文件中的出现次数（注意 Spring 组件靠反射注册，引用数为 0 **不等于**死代码，需人工确认）。
2. **接口对账**：提取全部 `@GetMapping/@PostMapping/@PutMapping/@DeleteMapping` 路径，与 `web/src/api.js` 中的实际请求地址逐一比对。
3. **前端方法级对账**：对 `api.js` 导出的每个 API 方法，在 `views/` 与 `layouts/` 中做精确匹配（带 `(`，避免 `create` 误命中 `createBatch`）。
4. **资源扫描**：`views/*.vue` 与 `router.js` 的路由表比对，识别未注册页面；检查其 `import` 的依赖模块是否存在于 `api.js`。
5. **配置闭环验证**：对每个播种的配置 key，反向搜索业务代码中的读取点（`ConfigService.get` / `SystemConfigRepository.findByKey`）。
