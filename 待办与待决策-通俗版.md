# 待办与待决策 · 通俗版

> 面向非技术的说明。做完的只列一行，重点在"还没动的"和"要你拍板的"。
> 详细技术依据见 `总方案-优先级与日历重构研判.md`。

---

## 〇、已经做完的（一句话回顾）

| 已完成 | 说明 |
|---|---|
| 四份规划文档 | 审计、修复方案、内置管理员方案、总方案（优先级总表 + 判重 + 日历研判） |
| **重复录入判重（P1-3/4/5/6）** | 同一个人同一天，记录的时间段不能重叠（包含、被包含、交叉都拦）；跨模块也管（加班转休 ↔ 调休使用） |
| ✅ **P0-1 密钥外置 + 启动自检**（2026-09-11 上线） | DB 口令与 JWT 密钥改由环境变量提供，仓库里不再有真凭据；缺失或仍是占位值则**拒绝启动** |
| ✅ **P0-2 / D1② 写锁改造**（2026-09-11 上线） | 删掉"全场等 3 秒"的冷却；锁改为最多等 3 秒，超时返回 429「系统繁忙」而不是无限阻塞 |
| ✅ **D1① / P1-7 数据库兜底**（2026-09-11 上线） | 两张记录表新增判重唯一键 + 唯一索引，**"完全相同"的重复数据在数据库层就插不进去** |
| ✅ **P0-3 启动不再自动改表**（2026-09-11 上线） | `ddl-auto` 由 `update` 改为 `validate`：表结构与实体对不上就启动报错，不再静默改表 |

**其余仍未动。** 下面是完整清单，已完成的项已标注 ✅。

> 本次上线附带：新增 25 个自动化用例（判重边界、写锁行为、判重键格式），全部通过。

---

## 一、P0：安全地基（3 项，✅ 已于 2026-09-11 全部上线）

| 编号 | 通俗说法 | 改造前什么情况 | 已改成 |
|---|---|---|---|
| **P0-1** ✅ | 钥匙和数据库密码直接写在代码里 | 配置里明写着数据库账号密码是 `root/root`；签登录令牌用的密钥也是写死的 | 真凭据全部搬出仓库，改由 `env.local.bat` 里的环境变量提供；启动时自检，**缺变量或还用占位密钥就直接拒绝启动** |
| **P0-2** ✅ | 一个人卡住，全站跟着干等 | 写操作锁是**无限等待**，且每次写之间强插 3 秒冷却（全系统上限约 20 次/分钟） | 冷却已删除；锁改为**最多等 3 秒**，超时直接返回 429「系统繁忙，请稍后重试」，不再无限阻塞 |
| **P0-3** ✅ | 每次启动自动改数据库结构 | 启动时程序会**偷偷改表**（加字段、改类型），改坏也不报错、没法回退 | 改为**只校验不改表**：结构与代码对不上就启动报错，问题从"静默漂移"变成"当场暴露" |

---

## 二、P1：会出错和会漏的（原 7 项，已完成 2 项）

> "重复录入"与"数据库兜底"两项已完成，剩下 5 项。

| 编号 | 通俗说法 | 现在什么情况 | 不做的后果 |
|---|---|---|---|
| **P1-1** | "必须改密码"只是装装样子 | 前端会跳转到改密页，但**后端根本不检查**。有人绕过页面直接调接口，就能一直用默认密码 | 默认密码形同虚设，等于没设这道防线 |
| **P1-2** | 收回权限不当场生效 | 你把某人的角色撤了，但系统**最长 24 小时内还认他旧身份**（令牌里存着旧角色） | 辞退/降职的人，还有一整天窗口期能操作 |
| **P1-7** ✅ | 数据库层没有最后一道拦网 | ✅ **已完成**：两张表新增判重唯一键 + 唯一索引，并回填了全部历史数据（427 条加班 / 63 条调休全部覆盖）。**"完全相同"的重复数据在数据库层就插不进去**；应用层判重失效时仍有这道兜底。注：MySQL 无法表达"时间段不重叠"，所以"包含/交叉"仍靠应用层 |
| **P1-8** | 报错把内部信息甩给用户看 | 出错时会把数据库报错原文、代码类名、文件路径直接显示在前端 | 攻击者能据此摸清系统内部结构 |
| **P1-9** | 跨域默认"全放行" | 代码里默认值是 `*`（允许任何网站来调），且允许带凭证 | 一旦配置漏填，任何网站都能借用你的登录态操作接口 |
| **P1-10** | 上传文件几乎不检查 | 只限 20MB，不查文件类型、不查有多少行 | 恶意的大文件（压缩炸弹）可能把内存吃光 |

---

## 三、还没动的 —— P2：加固（4 项）

| 编号 | 通俗说法 | 现在什么情况 | 不做的后果 |
|---|---|---|---|
| **P2-1** | 谁都能把管理员账号"锁死" | 登录失败锁定**只看账号名**，连错 5 次锁 15 分钟 | 有人故意连错 5 次 admin 的密码，就能**让所有人 15 分钟登不进去** |
| **P2-2** | 点了"退出"其实没退出 | 退出只是清了浏览器本地缓存，**那张通行证在 24 小时内仍然有效** | 在公共电脑上退出后，别人还能拿它继续操作 |
| **P2-3** | 接口说明书挂在网上 | Swagger 接口文档的部分静态文件（js/css/json）**无需登录就能拉取** | 相当于把系统有哪些接口、怎么调，公开挂出去 |
| **P2-4** | 系统配置页是个摆设 | 页面上 6 个开关**能存能改，但业务代码从来不读这些值**（折算系数其实是写死在代码里的） | 管理员以为改了配置生效了，其实一点没变——白改，还埋下"数据对不上"的隐患 |

---

## 四、还没动的 —— P3：清垃圾（2 项，零风险纯收益）

| 编号 | 通俗说法 | 现在什么情况 |
|---|---|---|
| **P3-1** | 删掉没人用的文件 | 13 个文件、约 1441 行代码**完全没有人引用**（旧的 `UploadController`、`ExcelService`、字典 Dict 全家桶、旧审批表 `LeaveRequest` 等） |
| **P3-2** | 删掉没人调的接口 | 12 个后端接口 + 10 个前端方法**没有任何地方调用** |

> 已核实：这些文件确实还在。删掉运行时**零变化**，纯粹让代码库更干净、攻击面更小。

---

## 五、还没动的 —— P4：内置管理员（5 项，其中 2 项已实现）

| 编号 | 通俗说法 | 状态 |
|---|---|---|
| **P4-3** | 管理员不参与任何工时计算 | ✅ **已实现**（现有逻辑已排除 ADMIN/CLERK） |
| **P4-4** | 管理员可多设备同时登录、互不踢下线 | ✅ **已实现**（登录时不重置令牌版本） |
| **P4-1** | 管理员永久内置、权限恒定、密码固定不可改 | ❌ 未做。**用户表上没有"内置"标记**；权限靠角色绑定，一旦角色被动过权限就跟着变；而且现在 `必须改密=true` 和"不许改密"是**直接冲突**，照原样做会把管理员锁死 |
| **P4-2** | 管理员不能删除/冻结自己 | ❌ 未做。**现在没有这道保护**——如果只有一个管理员，他把自己删了，系统就再也进不去了 |
| **P4-5** | 管理员不在用户列表里显示 | ❌ 未做。下拉框和报表已经排除，但**用户管理列表里还能看到他** |

---

## 六、还没动的 —— P5：日历重构（3 项，探索性）

| 编号 | 通俗说法 | 现在什么情况 |
|---|---|---|
| **P5-1** | 日历从"三种"简化成"两种" | 现在是 法定节假日 / 法定工作日 / 休息日 三种，还要推算"补班日"。想简化成 **A 法定工作日 / B 法定休息日** 两种 |
| **P5-2** | 折算系数改成可配置 | 系数（工作日 0.5、休息日 1）目前**硬编码在 3 个不同的代码文件**里，改一处漏一处就会"界面显示一个样、实际算法另一个样" |
| **P5-3** | 调休录到休息日要提醒 | 现在录调休**完全不管那天是不是休息日**，录了就直接扣。希望录到休息日时提示一下、用户坚持才放行 |

> 结论：**这件事技术上可行、且对现有数据零影响**（因为"法定节假日"和"休息日"系数本来都是 1，合并后每个日期的系数一个都没变）。但建议按顺序最后做，因为它和 P2-4、P5-3 是同一块代码，应该合成一次改。

---

## 七、✅ 决策进展（7 项全部已定，D1 三层方案已全部上线）

### 7.1 你已确认的 6 项

| 编号 | 事项 | 你的决定 | 落地时要注意什么 |
|---|---|---|---|
| **D2** | 系统配置页 | **A 接上业务** | 让配置页真正控制折算系数等，"改了立刻生效"。与 **P5-2** 合并做（同一块代码） |
| **D3** | 退出登录 | **B 只退当前设备** | 只让当前这台失效，其他设备不受影响。**需要新增一套"令牌黑名单"**：把"当前这张通行证"作废，而不是把该账号所有通行证都作废 |
| **B1** | 内置管理员默认密码 | **B 环境变量** | 与 **P0-1** 一起做，代码里不出现密码 |
| **B2** | 管理员登录锁定 | **A 豁免** | 管理员锁不死；**必须同时补 P2-1 的 IP 限流**，否则等于给暴力破解开绿灯 |
| **B3** | 系统日志中的管理员操作 | **A 保留** | 一视同仁，便于审计追溯 |
| **B4** | 管理员前端显示 | **A 纯隐藏** | 用户管理列表完全不出现。见下方"B4 的小提醒" |

> **B4 的小提醒**：选"纯隐藏"后，`admin` 这个用户名**仍然被占用**。如果有人不知情、想新建一个叫 `admin` 的账号，会收到"用户名已存在"，但列表里又看不到那一行 —— 容易让人困惑。
> 建议顺手加一句提示：**创建用户时若命中被隐藏的内置账号，提示"该用户名为系统内置账号保留，不可重复创建"**。这只是 1 行代码，不影响"不显示"的效果。

### 7.2 D1 —— 写锁：先纠正你的一个关键判断 ✅（方案已全部上线，以下为决策依据留档）

**你问：是"所有人一起等"，还是"数据一样才等"？**

→ **是前者，而且比你想的更严重：是"所有人无条件一起等"，跟数据是否相同毫无关系。**

现在的真实机制（已核对代码）：
1. 全系统**只有一把锁**，所有人做增删改都要抢它；
2. 抢到之后还要**等"距上一次写操作完成满 3 秒"**才能开始；
3. 不管两个人录的是不是同一个人、同一天、数据是否相同 —— **一律排队，且每次写之间各插 3 秒**。

**也就是说：全系统理论最大写入速度 = 20 次/分钟。** 10 个人同时录数据，就是一条一条排队、每条之间隔 3 秒。**"3 秒冷却"才是"所有人都在等"的真正元凶**——锁本身每次只持几毫秒，根本不是瓶颈。

> 你说"那这个校验直接变成：有这条数据就不允许录" —— 方向对，但**光这样不够**。

⚠️ **关键点：光"先查一下有没有、有就不让录"是拦不住的。** 两个请求可能在**同一瞬间**都查到"还没有"，然后**都去插入** —— 这就是竞态，重复数据照样进库。要真正防住，只有两条路：**要么让"查"和"写"之间不被插队（串行化），要么让数据库层根本插不进去。**

### 7.3 D1 推荐方案（三层，逐层加固，每层都可独立上线）

| 层 | 治什么 | 具体做法 | 效果 |
|---|---|---|---|
| **第 1 层：数据库硬约束** ⭐ | **"完全相同"** | 加唯一索引。为兼容"作废后还能重录"，增加一列 `active_key`：记录有效时 = `人-日期-模式-开始-结束` 拼串，作废时置为 NULL（MySQL 唯一索引允许多个 NULL） | **即使程序判重失效、两个请求真的撞在一起，数据库也插不进重复数据。** 这是最硬的一层，正好兜住你最担心的情况 |
| **第 2 层：删掉 3 秒冷却 + 有界等待** | **"全场干等"** | ① 删除"写后冷却 3 秒"这段逻辑；② 把无限等待改为 `tryLock(3秒)`，超时返回"系统忙，请重试" | 正常录入几乎零等待。**全局锁保留**（每次只持几毫秒，吞吐完全够），"先提交的赢"因此依然成立 |
| **第 3 层：程序判重**（**已完成**） | **"包含/交叉"这类非完全相同的重叠** | 已实现的 `RecordConflictGuard` | 数据库管不了"部分重叠"，这层必须保留 |

**为什么第 2 层不直接改成"按人分锁"？**
"按 人+日期 分锁"（不同的人真正并行）确实更优，但**代价大**：锁必须盖住"检查 → 写库 → 提交"整个事务，而现在的锁在拦截器里、事务边界在 Service 层，要改就得把锁下沉并把方法拆一层（非事务外壳持锁 → 调用事务内核）。而**全局锁每次只持几毫秒**，在这个系统规模下根本不会成为瓶颈。
**结论：先做前两层；等真的出现"录入高峰排队"再考虑分片锁。**

### 7.4 三层方案的落地清单 ✅（2026-09-11 已全部落地）

| 动作 | 对应现有编号 | 状态 |
|---|---|---|
| 加 `active_key` 列 + 唯一索引 | **P1-7**（数据库兜底） | ✅ 已上线：两张表已建唯一索引，存量 427 条加班 / 63 条调休数据全部回填，零空值；实测重复插入被数据库拒绝（1062） |
| 去掉 3 秒冷却 + 改有界等待 | **P0-2**（写锁改造） | ✅ 已上线：冷却已删，锁改为 `tryLock(3s)` 超时返回 429 |
| 保留跨模块区间判重 | 已完成 | ✅ 无需改动 |

**后续顺序：P1 剩余 → P2 → P4 → P5（P3 随时可做，零风险）。**

---

## 八、其他已定事项（供你对照，不用再答）

| 决策 | 结论 |
|---|---|
| E1 其他转休互斥口径 | ① 同人同日仅一条，与加班转休互不干扰 |
| E2 跨模块判重 | 需要，按时间区间重叠判定（已完成） |
| E3 时间端点相接 | 不算重复（15–17 与 17–19 可以并存） |
| E4 日历重构时机 | 按 P0→P5 顺序，最后做 |

---

## 九、本次上线结果 + 下一步

### 9.1 已上线（2026-09-11）

按你"按你的改，改完跑用例测试，然后上线"的指示，本轮全部完成并已部署：

| 项 | 做了什么 | 验证结果 |
|---|---|---|
| **P0-2 / D1②** | 删掉写后 3 秒冷却；锁改为最多等 3 秒，超时返回 429 | 单元用例通过；服务实测正常 |
| **P0-1 / B1** | DB 口令、JWT 密钥改由环境变量提供（`env.local.bat`，已加入 `.gitignore`）；启动自检缺变量/占位密钥即拒绝启动 | 启动自检通过（弱口令仅告警不阻断） |
| **P0-3** | `ddl-auto` 由 `update` 改 `validate`，不再静默改表 | 启动通过，实体与表结构一致 |
| **D1① / P1-7** | 两张记录表新增 `active_key` 列 + 唯一索引，历史数据全部回填 | 存量零空值；直接插入重复数据返回 `ERROR 1062`，数据库层判重生效 |
| **P1-7 附带** | `GlobalExceptionHandler` 捕获唯一键冲突，返回友好提示 | — |
| 用例 | 新增 25 个自动化用例（判重边界 / 写锁行为 / 判重键格式） | **25/25 全部通过** |

> 上线后服务状态：端口 **8600** 监听中，`GET /api/auth/health` 返回 `{"code":0,"message":"ok"}`，首页 `200`。

### 9.2 下一步（等你发话，不自动开工）

- **剩余 P1**：P1-1（强制改密后端校验）、P1-2（权限变更即时生效）、P1-8（错误信息脱敏）、P1-9（跨域默认收紧）、P1-10（上传校验）。
- **P2**：其中 **P2-1（登录 IP 限流）是你选 B2 时点名要一并做的**，建议优先。
- **D3**：退出登录只退当前设备 —— **方案已定（令牌黑名单），代码尚未实现**。
- **D2 / P5-2**：配置页接上业务（折算系数可配置）—— 已定，未实现。
- **P3 / P4 / P5** 其余项。

一句话告诉我下一批做哪些即可（例如"**做 P1 剩余 + P2-1**"）。
